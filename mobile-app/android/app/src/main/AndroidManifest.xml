<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- allowBackup=false: Android's OS backup is OFF for Shape.
         Excluding only app_webview/ left everything else in the sandbox
         (SharedPreferences written by Capacitor and its plugins, files/,
         databases/) still going to the user's Google Drive — so Google was a
         recipient of app data for a health app, with no way for us to bound
         what ended up there. Nothing is lost by disabling it: all real state is
         server-side, and what is local is the auth session plus caches, which a
         restore must NOT resurrect on another device anyway.
         The two rules files below are kept deliberately as defense in depth —
         they take effect if allowBackup is ever flipped back to true. -->
    <application
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- In-app video calls (Jitsi embed): camera + microphone -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Live heart-rate monitor (Bluetooth LE straps/watches, services/hrm.js).
         API 31+ uses SCAN/CONNECT (scan never derives location); the legacy
         pair covers Android 11 and below. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />

    <!-- Background audio prep: FOREGROUND_SERVICE is required to declare a
         foreground service that keeps the WebView audio stream alive when the
         app is backgrounded on Android. A foreground service + a background-mode
         or foreground-service Capacitor plugin (e.g. capacitor-plugin-background-mode
         or @capawesome-team/capacitor-android-foreground-service) must be wired in
         the native build for full Android background audio. This permission alone
         does NOT enable background audio — it is a prerequisite only. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
</manifest>
