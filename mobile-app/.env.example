# Copy to .env and fill in. Vite exposes anything starting with VITE_ to the
# client bundle, so do NOT put service-role keys here. Server-side calls
# (refunds, Stripe webhooks) stay on the Next.js web app.
VITE_SUPABASE_URL=https://zznufekgjngecelwxndw.supabase.co
VITE_SUPABASE_ANON_KEY=
VITE_API_BASE_URL=https://theshapecommunity.com
