# Session handoff — 2026-09-01

## 0. READ THIS FIRST

This covers **fourteen merged PRs, #1992 → #2006** — everything after
[`HANDOFF-2026-08-31b.md`](HANDOFF-2026-08-31b.md), which **shipped as #1991** and closed
at #1990. Eight of them are the dependency / CI wave (§2a); six are the owner-reported
product work (§2b), and one of those turned out not to be the bug it looked like.

⚠ **THE BOUNDARY WAS WRONG IN THE FIRST DRAFT OF THIS FILE, AND THE REVIEWER CAUGHT IT.**
It read *"six merged PRs, #2001 → #2006, everything after #1990"* — true of the six, false
as a boundary, because #1992–#1998 and #2000 sit in the gap. `WORKLOG.md` is auto-loaded,
so a handoff that claims a range it does not cover makes the next session believe that
work was handed off. **A handoff's range claim is a claim** — derive it from
`git log origin/main`, not from the PRs you happen to have worked on. (#1999 is in the
numbering and **not** in the range: Dependabot opened it and it was closed unmerged,
superseded by #2000.)

**The three rules the last handoff obeys, still binding:** no `main` SHA is recorded
below (a handoff is committed *before* its own squash, and a `claude/*` checkout's
`HEAD` reports the branch); "nothing in flight" cites the **open-PR list**, never
branch-commit counts; and the suite figure is a **re-run**, never carried forward.

⚠ **THE HEADLINE FINDING IS THAT THE FEATURE WAS NEVER MISSING.** The owner reported not
having seen the pre-app *"Want music while you move?"* prompt in a while. The prompt has
been on the launch path the whole time — `BSRadioPrompt` renders whenever `showPrompt`
is set, it is mounted in the client shell **and both coach shells**, and the
Settings → Shape Radio toggle governing it already existed. **What was broken was
"once".** Read the source before assuming a surface was removed; the gate, not the
render, was the defect.

---

## 1. State snapshot

| | |
|---|---|
| `main` | read it: `git fetch origin main && git rev-parse HEAD origin/main` — ⚠ **not** the `--short` form, see §4 |
| Working branch | `claude/handoff-docs-onboarding-4axfk9` — re-synced to `origin/main`, this handoff on top |
| Open PRs | **none but this handoff's own.** Measured before it opened: `list_pull_requests` returned `[]`. ⚠ Re-run the list; a handoff's own PR makes this row stale for exactly one entry. |
| Suite | **2640 / 2640**, 0 fail (re-run this session) |
| `tsc --noEmit` | exit 0 — ⚠ **except one pre-existing local-only error**, see §4 |
| Migrations owed | **ZERO. None was written this session** — the ask-gate rides `user_goals('client_settings')`, an existing whole-doc store. |
| i18n | 13 locales × 18 namespaces × **4,161** `en` keys = **54,093** values (was 4,162 — #2002 is a net −1: one key added, two retired) |

### The i18n ratchet (`tests/i18n-surface-inventory.test.mjs`, 9/9)

| | |
|---|---|
| components rendering JSX | **359** |
| fully covered | **115** |
| partial | **34** (168 strings left) |
| no translator at all | **96** (818 strings) |
| renders no user copy | **114** |

**Every column unchanged from the last handoff — and that is the certification, not an
omission.** #2002 swapped one keyed string for another (`home:edition.clients` →
`home:dateline.edition`), so a presentation change had to move the measurement by
**nothing**. A ratchet that moved here would have meant copy was added or dropped.

---

## 2. What shipped

### 2a. The dependency / CI wave — #1992 → #2000, eight PRs

Not this session's product work, but inside the range and therefore this handoff's to
record. None touched app behaviour.

| PR | Head | What |
|---|---|---|
| #1992 | `476546b` | Deleted the retired WORKLOG banner block, after folding in what it alone held |
| #1993 | `d362645` | deps(actions): `actions/checkout` 7.0.0 → 7.0.1 |
| #1994 | `64ee4ee` | deps(actions): `actions/setup-java` 5 → 6 |
| #1995 | `c6c8612` | deps(mobile): the mobile-deps group, 8 updates |
| #1996 | `912782d` | deps(web): the web-deps group, 4 updates |
| #1997 | `0bbb107` | ci: SHA-pin the `android-build` actions; correct the dependabot-majors comment |
| #1998 | `5e074ad` | deps(actions): `gradle/actions/setup-gradle` → v6.3.0 |
| #2000 | `6dfca9e` | Ignore `@sentry/react` in `mobile-app` — `@sentry/capacitor` pins it **exactly**, so Dependabot's bump (#1999) could only ever conflict; #1999 was closed unmerged rather than forced through |

### 2b. The owner-reported product work — #2001 → #2006, six PRs

| PR | Head | What |
|---|---|---|
| #2001 | `60897c60` | About signature → **Christopher Perry** (mobile `BSAboutPage` + website `about.jsx`, alt text included) |
| #2002 | `f72d6490` | Radio prompt asks **once per account**; client Home masthead joins the coach dateline |
| #2003 | `e430dfed` | The two stale ratchet comments the rename left |
| #2004 | `1baa280c` | The WORKLOG 2026-09-01 entry |
| #2005 | `cf9ea534` | The ask-gate **closes a prompt already on screen**, not just the flag behind it |
| #2006 | `04b1c59d` | Amended that entry with #2005's defect |

#### #2002 — the radio ask-gate, and the four decisions behind it

The gate lived in **one device-level localStorage record** (`shape.radio.pref.asked`),
so a reinstall or a second device re-asked a member who had already answered — and on a
shared device whoever answered first answered for everyone. It is now a property of the
**account**: a per-uid localStorage mirror for the first synchronous render, converged
from `user_goals('client_settings').radioAsked`.

`shape.radio.pref` is **untouched and stays device-level** — it carries the runtime
on/off and deliberately survives sign-out. Only the ask-gate moved.

⚠ **SIGNED-OUT IS NEVER ASKED, AND THAT IS A FINDING, NOT A STYLE CALL.** Playback is
licensing-gated to a signed-in account (`bsRadioSignedIn`, the non-interactive
boundary), so a preview visitor answering "yes" gets silence. Worse, under the old
device gate that **unanswerable prompt CONSUMED the ask** — so the real account they
went on to create was never asked at all. The effect keys on `authTick` as well as the
gate, because the radio provider mounts **above** the async auth gate: on a cold launch
there is no uid on the first evaluation, so it must fail closed and re-run.

⚠ **THE FLAG IS STICKY-TRUE, SO THE HYDRATE *ORs* RATHER THAN CONVERGES.** Nothing ever
writes `false`. A converging hydrate would let a stale or absent cloud doc re-open a
prompt the member already answered; ORing means a mirror **ahead** of the cloud instead
re-issues the write — the retry, for free. `getUserGoals` resolves **null** for every
can't-know case (no backend · not signed in · query error — it never rejects) and `{}`
for a genuinely absent row, so a null read keeps the seed and the persist declines
rather than clobbering.

⚠ **NO MIGRATION FROM THE LEGACY DEVICE FLAG, DELIBERATELY.** `shape.radio.pref.asked`
is not attributable to any account — on a shared device it is whoever answered first —
so reading it as *this* account's answer is exactly the cross-account class the per-uid
keys exist to prevent (the `_followCache` lesson). **The cost is one re-ask per account
after ship**; the alternative is silently never asking someone, which is not
recoverable.

⚠ **`client_settings` IS A WHOLE-DOC UPSERT.** `bsSettingsWriteSerial` is now
window-exposed and the radio module's persist runs through it, falling back to a direct
read-merge-write since the radio module loads **before** the role bundle and must not
hard-depend on order. The write is **bound to the initiating uid** — `saveUserGoals`
resolves the user at SAVE time, so an account switch mid-flight would write A's whole
settings blob into B's row; a changed or unresolvable identity discards it and the next
hydrate re-issues. The Settings pane's own save **folds the gate** exactly as it already
folds `onlineRail`.

#### #2005 — the defect inside my own gate, found by audit not by report

The auto-prompt fires on a **600ms timer** while that cloud read is still in flight.
Inside 600ms the effect's own cleanup clears the timer and nothing paints — but on a
second device with a slower round trip the prompt is **already on screen** when
`radioAsked` comes back true, and the branch only flipped the flag behind it. A member
who had answered on another device was asked again, which is the one promise this whole
gate exists to keep. The cloud-true branch now **dismisses the prompt as well as setting
the gate**.

⚠ **The prompt is deliberately NOT held until that read settles.** Waiting fails toward
never asking a genuinely new member on a dead network, which is **not recoverable** —
the same direction as the no-migration call above. Asking twice in a rare slow-read
window is, and the dismiss closes it anyway.

The same audit caught **a sentence of mine that overclaimed**: the comment read
*"Nothing here can set the gate back to false"*, which is false as written —
`setAsked(seeded)` on the very next line does exactly that. That is deliberate (the
effect is keyed on identity, so a re-seed means the account changed and B must not
inherit A's answer); the true claim is the narrower **no cloud read can lower the gate**.
*A because-clause is a claim with a shelf life* — this one was wrong the day it was
written.

#### #2002 — the client Home masthead

Home carried the wordmark, then a separate `PAPER2` *"Clients Edition · No. 14 / Vol. I"*
strip **below the ticker**; `BSProToday` §A.2 carries a single hairline dateline row
directly under the wordmark. Home carries that row now and the strip is gone; the markup
matches the coach's byte-for-byte on padding, border, flex, sizes, weights, tracking,
uppercase, colors and `tabular-nums`.

⚠ **TWO DELIBERATE DIVERGENCES, BOTH STATED IN-CODE.** (1) The day and month come from
**this page's locale formatters** (`_dowShort`/`_monShort`), not the coach module's
hardcoded English `_BS_DOW`/`_BS_MON` arrays — a masthead must not print English weekday
tokens on a Spanish screen. (2) The clock derives from **`bsNowMin`**, the minute ticker
already running for the slate's NOW tick, so it advances while Home stays open instead
of freezing at the render that painted it.

---

## 3. Guards — two fixes worth more than the features

Both were found by **mutation-testing**, not by reading.

**(1) A guard broke for a reason it does not care about.** The Settings-write guard in
`tests/broadsheet-checkin-pref.test.mjs` pinned the **exact literal**
`{ ...doc, ...railFold, ...editedRef.current }`, so adding a second fold failed a test
about something else entirely — *the second time that assertion has broken for a reason
unrelated to its subject*. It pins the **invariant** now: doc spreads first, edited
spreads last, only `*Fold` between.

**(2) My own new assertion was hollow.** It matched the `askedFold = {…}` **DECLARATION**,
which still stands when the spread is deleted — so dropping `...askedFold` from the save
survived with **zero failures**. Re-anchored on the spread; both mutations then killed.
*A guard that reports a pass is a broken instrument until the mutation is proven to have
landed* — this file's own rule, paid for again, inside the guard written to enforce it.

**`tests/radio-ask-gate.test.mjs` DRIVES rather than greps** — it brace-matches the four
gate helpers out of the **shipped source** and evaluates them against a stubbed `window`
plus an in-memory localStorage, so an equivalent rewrite passes and a real regression
fails. 10 tests; #2005's is the tenth and asserts on the **branch, not the file**
(`setShowPrompt(false)` also appears in `answerPrompt` and `setRadioPreference`, so a
file-wide match passes with the branch left broken).

---

## 4. Two environment facts, so nobody re-derives them

⚠ **`npx tsc --noEmit` REPORTED A STRIPE `apiVersion` ERROR LOCALLY, AND THE FIX IS ONE
COMMAND THAT TOUCHES NO TRACKED FILE.** It reproduced on a clean `main` worktree, and the
cause was a stale local install: `package.json` declares `stripe: ^22.6.0` and the
lockfile pins 22.6.0 while `node_modules/stripe` reported **22.3.2**, predating a merged
Dependabot bump. CI runs `npm ci` and its Web check was green throughout.
⚠ **CORRECTED WITHIN THIS SESSION — the 2026-09-01 changelog entry that first recorded
this said “deliberately not ‘fixed’ here — a local environment artifact is not a change to
ship”, and the second half of that is right while the first half gave up too early.** The
distinction it missed is between the *lockfile* (a tracked file, and changing it WOULD be
a change to ship) and `node_modules` (untracked, and simply wrong). Resyncing the second
alone clears it: **`npm install stripe@22.6.0 --no-save`** — `git status` is unchanged
afterward and `tsc` exits 0. `npm ci` does the same job for the whole tree, slower.
**This is not optional housekeeping:** the pre-commit hook runs `tsc` on any commit
touching a `.ts` file, so a stale install blocks the commit outright, and the tempting
workaround (`SKIP_VERIFY=1`) also disables `npm test` — trading a one-command fix for a
disarmed gate. Resync, don’t bypass.

⚠ **`git rev-parse --short HEAD origin/main` FAILS** — `fatal: Needed a single revision`,
exit 128. With two revisions **`--short` is the part that breaks, not the pair**, so
`git rev-parse HEAD origin/main` works fine as a one-liner. This session **corrected it
at the source** in `docs/WORKLOG.md`'s stale-base bullet, which is the file every session
auto-loads and which had been prescribing the broken form. Three earlier handoffs
(`-06-16`, `-08-29`, `-08-31`) had each recorded the failure independently and told the
reader to run the refs separately — *a fix written only where nobody auto-reads it is not
a fix*, and all three diagnosed the two-ref form rather than `--short`, so the sharper
answer had to be re-derived a fourth time.

---

## 5. Open follow-ups

- **The owner on-device pass is the one thing this session could not do.** #2005's defect
  is a *second-device, slow-network* race — worth confirming on a real second device that
  a member who answered on device A is not asked again on device B.
- **Expect one re-ask per account after this ships**, by design (§2). If that reads as a
  bug in the wild, it is the documented cost of not migrating an unattributable device
  flag, not a regression.
- **The registered owner ruling still stands:** the house runs **one** reviewer by ruling
  (CodeRabbit retired 2026-08-24; *"never trigger Codex"* is the owner's ruling even
  though its stated premise — no credits — was refuted 2026-08-29). Neither reviewer
  posted on #2001–#2006; the merge gate held as **CI green on the final head AND not a
  draft**.
- Everything in [`HANDOFF-2026-08-31.md`](HANDOFF-2026-08-31.md) §4–§6 (the ranked next
  work, the seven owner rulings, the registered-not-built list) and
  [`HANDOFF-2026-08-31b.md`](HANDOFF-2026-08-31b.md) §4 (the marketing constraints, the
  SECURITY INVOKER freeze) is **unchanged by these six PRs** and remains the authority.

---

## 6. The other HANDOFF files

`ls -t docs/HANDOFF-*.md | head -3` — the newest three are this file,
[`HANDOFF-2026-08-31b.md`](HANDOFF-2026-08-31b.md) (#1988 → #1990) and
[`HANDOFF-2026-08-31.md`](HANDOFF-2026-08-31.md) (#1957 → #1986, the i18n wave).
