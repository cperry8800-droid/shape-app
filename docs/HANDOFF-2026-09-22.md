# Handoff — 2026-09-22 · the coach builders, picked and briefed

**Read first:** [`BUILD-2026-09-21-builder-grid-sheet.md`](BUILD-2026-09-21-builder-grid-sheet.md)
— the code-level brief for the build the owner has agreed to. Then
[`REVIEW-2026-09-21-coach-builders.md`](REVIEW-2026-09-21-coach-builders.md) §10.4 (the rulings),
§10.6 (the picked concept) and §11 (the colour system). The concept board the pick was made on:
https://claude.ai/artifact/TWHg2SbPj6VNeoeTXxtYpC — tab **G**.

## State

- Branch `claude/friendly-allen-gcpeud`, rebased onto `main` = `0e63de1`, pushed, **no PR open**.
- **No changelog entry exists for this work**, by the house rule: the entry comes after each PR.
- ⚠ **UPDATED 2026-09-22 — PR 1 AND PR 2 OF THE BRIEF ARE BUILT.** The brief's §10 and §11 are
  what each one actually found; its order table marks both SHIPPED. Read those two sections before
  starting PR 3, because between them they carry the whole hazard list for the remaining sweeps.
  What is on the branch now: the Workouts header's ampersand in Space Grotesk
  (`dashBuilder.jsx:605`, the owner's pick B) · the `dash.css` token block and the chrome sweep ·
  the six coach/client surfaces swept · **every hex-parsing helper in `public/newdesign` made
  token-tolerant**, which is PR 1's own class closed at the sink.
- **PR 3 is next**: Business · Goals · Progress · Train · Nutri · the profile extras · the two
  Settings pages — **491 literals** (derived, not carried; `publicProfile.jsx` alone is 190), with
  known hex-append sites at `dashProgress.jsx:275,505,1313` and `clientMeSettings.jsx:726,749`.
  PR 4 is **223**. ⚠ **AND 1,819 MORE ACROSS 59 FILES ARE IN NO PR LIST AT ALL** — the brief's
  four sweeps cover the dashboards, not the directory. The one that matters is the **chat bubble**
  (`chatWidget.jsx` 122 + `communityFeed.jsx` 120): it mounts on 35 pages including every
  dashboard, so as staged, a light dashboard would carry a **dark bubble over it**. Put that in
  PR 5's scope or get an explicit ruling that the bubble keeps the dark paper.
- **PR 5 is the half the owner asked to see**: the light paper values, the per-account switch, the
  Settings control, and flipping the default to light.

## The rulings, verbatim (2026-09-21)

- *"i like the toggle for both options. looks god"* → **G**, the Grid ⇄ Sheet switch, for both
  the trainer and the nutritionist builders.
- *"use the designs but keep the same colors currently on website"* → *"but i like the look of the
  white and what you already built"* → *"so maybe use that for now, but create the option to change
  back to the dark tones"* → **the light paper is the default for now; the website's current dark
  colours stay available as a switch, exactly as they are today.** The app's papers and accents
  on the website are a later step, on the same token layer.

## What the next session does

1. ⚠ **PR 1 and PR 2 are done — start at PR 3**, and read the brief's §10 and §11 first. The
   capture recipe is in §8 (the shells render in Chromium once React, ReactDOM and Babel are
   served from `npm pack` bytes, which match the pages' SRI hashes; fix the clock with
   `page.clock.setFixedTime`, seed `Math.random`, disable animations, capture through CDP
   `Page.captureScreenshot` — Playwright's own screenshot wait hung on the two coach *Today*
   tabs; `sharp` is in `node_modules` for the pixel compare).
   ⚠ **AND A PIXEL DIFF PROVES ONLY THE STATES THE HARNESS OPENS.** PR 1 was 41/41 identical and
   still shipped three broken surfaces, because the chat bubble's Feed tab, its profile card and
   Nora's row in site search are all closed in a page capture. The two AST guards in
   `tests/newdesign-paper-tokens.test.mjs` are what cover that now — run them, and do not read a
   green diff as a green sweep.
   ⚠ **The harness's noise floor is TWO named surfaces, and a control run once is a sample.**
   `NutritionistApp-schedule.png` differs between two captures of the identical tree by ~11k px in
   one 35px band, and `page-index.png` by **221,800 px** — the scroll-driven journey renders in two
   stable modes depending on run position. The first control **passed** the homepage, which is how
   a bimodal flake hides: compare those two position-matched (a vs a, b vs b) or not at all. Every
   other surface is byte-deterministic.
2. `dash.css` is linked with a hand-written `?v=` on 34 pages and the precompile hashes script
   tags only (`scripts/build-newdesign.mjs:263-276`); the brief has the stylesheet link joining
   the content-hashing rather than a 34-file `?v` sweep.
3. One front-loaded `@coderabbitai full review` per PR; the changelog entry after the round.

## Registered

- ⚠ **PR 3 OWNS A HAZARD NOTHING GUARDS: A COLOUR COMPARED BY EQUALITY.** `dashGoals.jsx` picks a
  spark colour with `accent === DGO_INK50` (three sites) and `shared.jsx`'s `ShapeWord` infers
  light-vs-dark with `c === "#fff" || … || c.includes("244,241,234")`. Both are safe **today**,
  checked rather than assumed — every `c:` in `dgoStateView` is the constant itself, never a
  literal spelling, and `ShapeWord` is handed a colour only by the retired exploration files,
  where the token and the hex fail every arm identically. PR 3 sweeps `dashGoals.jsx`: the moment
  one side is a token and the other a literal, the spark draws the wrong colour and nothing fails.
  The fix is a semantic flag (`muted: true`), not a colour compare.

- The three PR-plan questions the owner has not been asked and the brief defaults (its §7):
  the Grid opens first · Anybody + Schibsted on the dashboards · *"In sequence from start"*
  retired · the switch covers all three dashboards · the preview is the client's website surface.
- The website has **1,129 colour literals, 217 distinct**, across the dashboard modules and the
  shared shell, and no custom properties; `pageShell.jsx` renders on 69 pages, so its sweep must
  carry today's values as `var()` fallbacks or the marketing pages change.
