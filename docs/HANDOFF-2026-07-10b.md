# Handoff — 2026-07-10 (session B): The launch on the wire

Second 2026-07-10 session (the first shipped the Nora wave —
[`HANDOFF-2026-07-10.md`](HANDOFF-2026-07-10.md)). This one un-parked the
splash-pages rearrangement and shipped the ENTIRE launch flow into one visual
language — "the wire" — across six PR rounds in a single day, each round
driven by the owner's live on-device look.

## State snapshot

- `main` at **`b34d8a35`** (#1677), clean tree. Nothing in flight.
- All merged: #1666 (War Room item) · #1667 spec · #1668 build · #1670 ·
  #1671 · #1673 · #1674 · #1675 · #1676 · #1677. Branches kept.
- No migrations this session. No new routes. No env changes.
- `public/m` builds AT DEPLOY — a merge is live ~5–10 min after Vercel
  finishes; check the `/m/` asset hash before re-testing on device.

## What shipped (in order)

1. **#1667/#1668 — the core rearrangement** (spec → build): member cold open =
   **wire-ticker beat** (membership check resolves BEHIND it — the "Checking
   membership…" screen is dead) → **"The telegram"** (the member's real day as
   one wire dispatch, 5s self-advance + tap-skip, directive from the SAME
   source as Home) → app. Warm same-day relaunch skips straight to the app
   (per-account per-LOCAL-day `shape.dailySeen` stamp; boot uid via persisted
   `shape.lastUid` because auth hydrates async). Signed-out = beat → wire wall
   (tap-gated, `paywall_viewed` verbatim) → invite edition.
2. **#1670/#1671 — first look fixes**: hot INCOMING row off the wall/hold
   grounds (beat-only), dim 0.22, invite re-set in wire, beat dwell 1.1s→3.5s.
3. **#1673 — the flow goes ALL wire**: clean centered invite dispatch,
   LOADING readout, language picker + every auth screen (sign-in / join /
   phone / verify-email) onto the wire ground.
4. **#1674 — wording + the dietitian fix**: STOP/END tokens removed
   launch-wide; LOADING lower; the **RD/RDN toggle actually wired** — it was
   coercing dietitians onto the TRAINER application and never sending
   `details.nutrition_role` (fixed across screen init / submit values /
   `applicationToPayload`; the apply route reads it; reviewer contract
   unchanged).
5. **#1675 — beat structure + "The wire form" auth** (owner-picked from the
   concept board): the beat got composition (static centered INCOMING line —
   the drifting row used to clip mid-word; ticker rows spread full-height with
   an edge-fade mask; calm mark glow; mark centered; LOADING anchored low);
   sign-in/create rebuilt as the dispatch form — mono label column +
   dot-leader entries, TRANSMIT CTAs, create split **STEP 1 · IDENTITY → STEP
   2 · CREDENTIALS** (FILED recap; identity errors return to step 1; the
   Turnstile slot machinery mounts the widget on step 2 where the request
   fires). Input fixes: autofill pinned dark, `color-scheme: dark`, Turnstile
   `theme:'dark'` + `appearance:'interaction-only'`.
6. **#1676 — round 4 (owner on-device)**: mono topbars OFF the wall + beat +
   hold (footer rules stay; the invite keeps its dated topbar); the loading
   bar became a determinate **0→100% fill** (`bs-wire-fill`, 3.4s to match the
   dwell, parks full) sitting higher; the invite pitch gained "your nutrition"
   and its lines stepped up 11→12.5.
7. **#1677 — the provider application joins the wire** (both roles): the last
   paper-theme screen inside the launch flow, rebuilt fixed-dark on the shared
   ground (`BSWireGround` is now window-exported); role tag = the one
   role-heat placement (trainer rust / nutritionist gold, dietitian `· RD/RDN`);
   auth-style step register + teal segment strip; dot-leader fields
   (`bs-apply-*` classes: dark autofill pin, dark selects); squared teal
   chips/checks; dashed file drops; ghost BACK + clipped teal
   `TRANSMIT · SUBMIT →`. Prefill, validation (4 consents, 5-year minimum,
   10MB cap), the upload chain, and `nutrition_role` are verbatim.

## Architecture you'll need

- **`mobile-app/src/services/dailyWire.mjs`** (pure, 21 test vectors):
  `bsLaunchRoute` (warm-skip decision) · `bsAfterBeat` (lang/daily/gate
  routing) · `bsWireLines` (telegram assembly; null unless a signed-in member;
  absent legs OMIT their line) · `bsWireDirective` + **`BS_LEVER_HEADS`**
  (shared with Home's engineMove defaults — the two surfaces can't drift).
- **The wire grammar** (fixed-dark launch surfaces, never the paper theme):
  `WIRE_BG` teal-tinted gradient · mono `'JetBrains Mono', 'Cascadia Code',
  Consolas, monospace` · cream `#f2ede4` / teal `#34d6c5` · clipped-corner
  solid-teal CTA (`clipPath: polygon(0 0, calc(100% - 11px) 0, 100% 11px,
  100% 100%, 0 100%)`) · hairline mono ghosts · dot-leader entry rows
  (`.bs-wire-frow` / `.bs-apply-row`, dotted → solid teal on focus-within).
- **Key components** (`iosAppBroadsheetMain.jsx`): `BSWireGround({dim})` (the
  drifting dash field — evenly spread, edge-fade masked, window-exported),
  `BSShapeMark({calm})`, `BSWireLoading` (0→100% fill), the `wire-beat`
  BSSplash branch, `BSWireHold`, `BSPaywall`, `BSLogin` (the wire form),
  and `iosAppBroadsheetProviderApply.jsx` (the application).
- **Stage machinery** (`BSAppShell`): boot `bsLaunchRoute` → `'beat'` holds
  min 3.5s → `bsAfterBeat` on `!memberGateLoading`; telegram `onDone` stamps
  `bsDailyStamp(authUserId, bsLocalDay())`; logout clears `shape.member` +
  `shape.lastUid` + `shape.dailySeen`.

## Open follow-ups

- **OWNER on-device pass** (the standing item, now spanning the whole flow):
  cold open (beat → telegram, 5s advance + tap) · same-day warm relaunch
  (straight to app) · next-morning stamp rollover · signed-out wall + Preview
  → invite · the wire-form auth (create both steps × roles × RD/RDN toggle,
  phone OTP, autofill renders dark) · the application both roles ×
  4 steps · reduced motion (assembled, ENTER, no timer) · Black/Sage/Cream.
- The concept board (living record of the direction + picks):
  <https://claude.ai/code/artifact/562cd222-1012-4efc-9a2e-edadf0caefc9>
- Marketing-website self-serve copy (index/GetApp/Pricing) — still the open
  War Room item (#1666); the app wall line shipped with #1668.
- Carried from session A (Nora wave): OWNER Vercel env paste
  (`NORA_TTS_VOICE=sage` + `NORA_TTS_INSTRUCTIONS`) + the Nora on-device pass.
