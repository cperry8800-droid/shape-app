# Handoff — 2026-07-14 evening (the website performance + design-review wave)

Session handoff per the WORKLOG convention. Read alongside
[`docs/WORKLOG.md`](WORKLOG.md) (the dated 2026-07-14b entry) and the morning's
[`HANDOFF-2026-07-14.md`](HANDOFF-2026-07-14.md).

## State snapshot

- **`main` at `2809bf64`**, clean tree; session branches kept per house rule.
- Suite **632/632** · CI green + CodeRabbit run on every code PR (all findings
  addressed or refuted with reasoning) · six PRs merged.

## What shipped (6 PRs)

Owner asked for a website design/flow review + "reduce the delay" — the review
found the page-switch root cause and everything below followed from it.

1. **#1725** — nav dropdown close grace 500ms → 150ms (the only coded delay;
   the invisible hover bridge already prevents flicker).
2. **#1726 — the JSX precompile pipeline** (the big one): every page shipped
   `text/babel` scripts compiled IN THE BROWSER per navigation (~954ms
   main-thread on desktop localhost). `scripts/build-newdesign.mjs` now runs in
   the Vercel `buildCommand` (the `/m/` precedent): 73 shared jsx + 50 inline
   blocks → plain deferred scripts under `public/newdesign/nd/` (gitignored),
   Babel tag stripped, `?v` = content hash, `window.__ndCompiled` manifest for
   lazy loaders (globalChatButton's rich-chat boot uses it; Babel path = local
   dev). CI gate: `--check`. **Marketplace LCP 956ms → 273ms.**
3. **#1727 — perf diet**: production React UMD ×73 pages (SRI recomputed);
   heroes `Training 2/Nutrition 3/Rewards` 5–6MB PNG → 67–95KB WebP; brand
   logos → 3–4KB `shape-logo-nav-*` (they render at 50–64px); hover-prefetch
   in pageShell (strict `a.origin` guard).
4. **#1728 — the CLS root cause**: NOT fonts/images — the shared footer mounts
   from the first script while `#root` is empty, paints at viewport top, then
   is shoved ~5,000px down (the whole 0.71). `mountSiteFooter` now reserves the
   first viewport (`#root` min-height:100vh). **CLS 0.712 → 0.001** (Fast 3G ×
   4× CPU). Plus the honest chat badge: the hardcoded fake "24" is gone —
   count only threads stamped `clientChatTabs.__live === true` (nothing stamps
   it on marketing pages, so signed-out never shows a badge).
5. **#1729 — design-review fixes**: mobile `<760px` redirect is HOME-only;
   deep links render with a slim dismissible GET IT → banner (fixed 48px, in
   flow pre-paint = no CLS; injects a style shifting `header.shape-header`
   down 48px while present); SHAPE ◂ RADIO nav wordmark = typeset mono + teal
   + live dot (PNG dropped); Marketplace inactive toggle 0.32 → 0.66 ink +
   text-shadow scrim; nav links 13px/0.78.
6. **#1730 — mobile app** (owner pick A off the live concept board
   <https://claude.ai/code/artifact/2fc283cc-98cf-4e03-8451-78a51da180fd>):
   the Community six-chip type row dies; a mono **ALL ▾** native select rides
   the SHAPE/CLIENT/COMMUNITY row; the UNIVERSAL/FOLLOWING lens goes
   10.5px/800 baseline-aligned with the title (bsSubTab's 10px bottom padding
   was the float).

## Architecture you'll need next time

- **Adding/changing website jsx**: nothing new needed for the pipeline — the
  manifest and compilation are automatic. Changing `pageShell.jsx` still means
  a `?v` bump on 69 pages (now at `20260714d`); `globalChatButton.js` at
  `20260714c`.
- **`git ls-files --eol` before any multi-file sed in `public/newdesign/`** —
  17 HTML files are CRLF-tracked (not 2); MSYS sed strips their `\r`
  (restore: `sed -i 's/$/\r/'`). Quote paths — two pages have spaces.
- **The badge/live contract**: any surface that populates real chat threads
  must set `window.clientChatTabs.__live = true` or no unread badge shows.
- **CodeRabbit rate limits**: org cap hit twice; re-trigger with
  `@coderabbitai review` after the window. Its P1/P2s this wave were real
  (lazy-boot Babel dependency, demo-thread badge, banner under fixed header) —
  one false positive refuted with reasoning (supportBubble `?v` never existed).

## Open follow-ups

- **OWNER on-device / on-deploy pass**: page-switch feel across the site
  (production React + precompile only manifest on DEPLOYED pages — local raw
  serving still uses Babel); mobile deep-link banner on a real phone; the
  Booth/appearance pass from the morning handoff still stands.
- The **1-hour-cache CDN JS** (react/babel from unpkg) could move to
  self-hosted vendored files alongside the [[supabase-cdn-sri-followup]] SRI
  sweep — one less third-party origin.
- Concept-board pattern worked well for the filter pick — reuse it.
