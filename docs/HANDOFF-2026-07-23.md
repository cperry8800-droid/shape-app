# Handoff — 2026-07-23 · Coach box + profile customization: the SPEC phase

**State:** both wave specs are MERGED on `main` — **#1815** coach listing media
(`864b5a3b`) + **#1816** profile customizations (`905d3728`). **⚠ THE BUILD IS
OWNER-PAUSED** — "pause before you start build" is a hard gate; no build PR
(box A/B, profile C/D/E) starts without the owner's explicit go. This records
pass (WORKLOG · this handoff · War Room · memory) was itself owner-ordered:
"update worklog.md, memory, handoff documents, war room once spec is done,
before build."

## What this wave is

Owner: coaches customize their **marketplace box** (photos of themselves +
their business, app + web), then **more customizations for the actual
profile**, for coaches AND clients. Concept board → owner picks:

- **E · "The Combo"** — the box: portrait + **cover as the background
  picture** + a ≤6-photo captioned studio gallery.
- **D · want-ad row thumbs** — ruled IN ("yes add D") after an initial pass.
- **ALL TEN profile items** ("add them all"): coach **P1** intro film · **P2**
  the Line · **P3** philosophy prompts · **P4** business card · **P5** wins
  wall; member **M1** the Wall · **M2** Start line · **M3** the Shelf · **M4**
  the Line · **M5** the Film.

Board: claude.ai/code/artifact/18fe47d3-7cd7-46b2-b25e-aa83bbda8f9e

## The two specs (the build contracts)

### #1815 — `docs/superpowers/specs/2026-07-23-coach-listing-media-design.md`

- **Data:** `listing_media` jsonb on BOTH provider tables (`trainers` +
  `nutritionists`) — the `monthly_offer` precedent; clears the
  `guard_provider_admin_columns` blocklist (verified); public read is free;
  both marketplaces already `select('*')`. Shape: `{ portrait, cover,
  gallery ≤6 {url, caption ≤80}, updatedAt }` (setter-stamped).
- **The canonical guard** `public/newdesign/listingMedia.mjs` (ONE
  implementation — mobile imports it, web loads it as `window.ShapeListingLib`,
  tests import it): `bsSafeMediaUrl` → `bsOwnMediaUrl(v, bucket, ownerUid)`
  (own Supabase storage origin · owner-folder path prefix · no userinfo/port)
  → `bsNormalizeListingMedia(raw, ownerUid)` — **ownerUid REQUIRED,
  fail-closed**, field-by-field gallery rebuild, **image-extension allowlist**
  (post-merge amendment — the bucket allows video for plan clips).
- **Renders:** featured 2-up grid → stacked full-width `MktComboCard`; D
  thumbs (~30×36 + same-size initials fallback) on want-ad rows; THE LISTING
  cover background + THE STUDIO station; web card cover band (the card already
  renders `c.cover`); web profile studio strip — **the clicked listing's role
  rides the profile URL (`&role=`)**, the provider-row fetches prefer it over
  `d.role` (post-merge amendment, dual-role coaches).
- **Editors:** mobile `BSProListingMediaSheet` (practice shortcut after
  Monthly offer) + web `dashProfileExtras.jsx` card. Uploads:
  `ShapeCoachMedia.upload(file, { prefix: 'listing' })` →
  `<uid>/listing/<epoch>-<random>.<ext>`, image mimes incl. heif, ≤10 MB.
- **PR A carries the CWE-862 live probe matrix** (own-write succeeds · pinned
  columns hold in the same write · non-owner zero rows, both tables) + exactly
  **13 i18n keys** ×13.

### #1816 — `docs/superpowers/specs/2026-07-23-profile-customizations-design.md`

- **Data:** new keys on the EXISTING `user_goals('profile_custom')` doc —
  visitors already get the whole doc via `get_public_profile` → **zero new
  RPCs/routes**. Keys: shared `line` (P2/M4) · shared `film` {url, caption}
  (P1/M5) · `wall` ≤6 (M1) · `startLine` {title, date} (M2) · `shelf` ≤4 (M3)
  · `bizCard` (P4) · `pinnedReviews` ≤3 ids (P5).
- **Guards:** per-key owner-folder allowlist via the SAME `bsOwnMediaUrl`
  (`wall`→community-photos · member film→**member-films** · coach
  film→coach-media); the per-key normalizer enforced at **editor + the
  trusted save wrapper + render** — **allowlisted to this wave's keys only**
  (legacy cover/stats/highlight/song/prompts/socials byte-identical, AC 9).
- **M2 dates:** strict `^\d{4}-\d{2}-\d{2}$` + **round-trip real-calendar
  check** (rejects `2026-02-31` — JS Date normalization must never fabricate
  a countdown); noon-anchored **local-calendar-day** diff; past → absent.
- **P5 wins wall:** stores ids only; **resolves by `id` + the immutable
  `coach_reviews.owner_id`** (records-round ruling — CodeRabbit correctly
  escalated past the slug-residual framing: the wall stamps "REAL REVIEW ·
  PINNED", so a slug collision rendering another coach's words would be a
  fabricated credential). PR D ships `2026-07-23-coach-reviews-owner-id.sql`
  (additive column + index + a **conservative backfill**: stamp only on a
  single slug→provider match whose provider row predates the review — the
  tenure check; no slug-history table exists, and a bare current-state join
  would reattach a previous slug-holder's reviews after a rename/reuse —
  plus the write-path stamp going forward); multi-match/unmatched/pre-tenure
  rows stay NULL, never render, and can't be pinned; resolve-or-vanish
  unchanged.
- **The wave's TWO migrations:** `2026-07-23-coach-reviews-owner-id.sql`
  (PR D, above) and `2026-07-23-member-films-bucket.sql` (PR E) — a
  DEDICATED bucket (video mimes only · 60 MB · owner-folder RLS). Widening
  community-photos was rejected: existing photo paths don't check `file.type`.
- **Documented residual (CodeRabbit formally withdrew the finding):**
  public-bucket object URLs stay fetchable by URL-holders after a visibility
  flip — the platform's existing media architecture (photo posts, covers,
  coach media identical); `can_view` gates discovery, not object fetch.
  **Platform-wide signed-media migration registered as a follow-on.**

## Review economy (how 9 rounds stayed cheap)

Batched fixes (one commit + one summary comment per round), Codex re-triggers
HELD until CodeRabbit settled, then exactly ONE pass per PR. One security
finding declined with rationale → CodeRabbit **withdrew it and recorded a
learning**. Post-merge Codex P2s (3 total) became spec amendments in the
records PR rather than new review cycles.

## Build plan (once the owner says go — Opus, per the Fable-writes/Opus-builds split)

1. **Box PR A** — migration + `listingMedia.mjs` + mobile (editor · combo
   cards · D thumbs · LISTING) + the probe matrix.
2. **Box PR B** — web (directory card + profile studio + dashboard editor +
   the `&role=` URL threading).
3. **Profile PR C** — member M1–M4 (Terrain renders + customizer sections).
4. **Profile PR D** — coach P1–P5 (Signal renders + the shared video player),
   carrying the `coach_reviews.owner_id` migration (the wins wall's binding).
5. **Profile PR E** — M5 the Film + the member-films bucket migration.

Every build PR: the full gate (parse · tsc · tests · /m/ build ·
build-newdesign --check · LF · tr-shadow · catalog parity ×13 · render-mount
proof · adversarial pre-push self-review · 4-step merge gate).

## Open

- **OWNER: the build go.** Nothing builds until it's given.
- **OWNER migrations** ride their build PRs (listing_media column, box PR A ·
  coach_reviews.owner_id, profile PR D · member-films bucket, profile PR E) —
  raw links per convention, not yet written.
- Registered follow-ons: platform-wide signed media · re-keying the general
  Reviews SECTION onto `coach_reviews.owner_id` (the wall already uses it;
  the section stays slug-keyed for now) · P3 render verification on Signal
  (Opus checks whether prompts already render there).
- Standing items unchanged from HANDOFF-2026-07-22 (on-device passes, human
  translation review, externals).
