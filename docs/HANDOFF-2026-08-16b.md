# Handoff — 2026-08-16 · PR #1888 (legal pages · Radio licensing · privacy mechanisms · the 18+ gate)

⚠ **Two handoffs carry this date.** This is the `b` file — PR #1888. `docs/HANDOFF-2026-08-16.md` is a DIFFERENT wave (the shared-device sign-out
follow-ups, #1891) that merged to `main` the same day; the two collided on filename
and were split rather than one overwriting the other.

Read this alongside `docs/WORKLOG.md`. Everything below is verified state unless it
says otherwise; where something is unverified it says so explicitly, because this PR
has repeatedly been bitten by confident claims that were never checked.

## State snapshot

| | |
|---|---|
| PR | **#1888**, open, mergeable, base `main`, 17 commits |
| Branch | `claude/radio-legal-gates` |
| Worktree | `C:\Users\cperr\shape-legalgate-wt` (NOT the main checkout) |
| Head | the commit carrying this file (`git log -1`) — the round-9 fix; **supersedes `53cdd22bf`** |
| vs `main` | 0 behind as of this writing — re-check before any push |
| CI on head | re-check; **GREEN** on `53cdd22bf` and `88ca063ba` (Web ✓ · Mobile ✓ · gitleaks ✓ · Tests ✓ · debug APK ✓; Agent Review `neutral` = advisory, signed release APK `skipped` = opt-in) |
| Codex | round 9 on `53cdd22bf` returned **one P1 — real, now FIXED**; re-triggered on the new head, **verdict PENDING** |
| Migration | `2026-08-15-profiles-dob-immutable.sql` **APPLIED + behaviourally verified on production** |
| Suite | **1542/1542**; `tsc` clean; `next build` exit 0 |

## Codex round 9 found a P1 — REAL, and now FIXED (was: "needs an owner ruling")

**Codex reviewed `53cdd22bf` and returned one P1** ([discussion_r3791731789](https://github.com/cperry8800-droid/shape-app/pull/1888#discussion_r3791731789)),
on the exact claim the trigger comment asked it to check. **I reproduced it — it is real:**

```
DOB 2008-08-17 | instant 2026-08-17T00:30:00Z
America/Los_Angeles    localday 2026-08-16 | adult locally: false | JS adult: true  | ADMITS A MINOR EARLY
America/New_York       localday 2026-08-16 | adult locally: false | JS adult: true  | ADMITS A MINOR EARLY
UTC / Europe/London / Asia/Tokyo / Pacific/Kiritimati                              | agrees
```

The UTC day runs **ahead** of every zone west of UTC, so there `isMinorFromDob` declares
adulthood **before the member's local eighteenth birthday** — up to ~11h early
(Pacific/Niue, UTC−11). The header's claim that the asymmetry "only ever refuses for one
extra day rather than admitting a minor early" was **false west of UTC**, i.e. the comment
ruled out the exact thing the code was doing.

**First pass (`88ca063ba`):** the false safety claim was removed and replaced with the
verified counterexample. A one-directional guarantee in a comment is worse than the bug,
because it tells the next reader not to look.

**Second pass — the behaviour itself is fixed: adulthood is asserted only once it is true
in EVERY timezone.** The calendar day is read at **UTC−12** (`ADULT_REFERENCE_OFFSET_MS`,
the "anywhere on Earth" convention) — one subtraction before the existing clamped
comparison. No tz database lookup, no extra query, no migration.

- **The cost runs in the safe direction and is bounded:** a member is refused for up to 12h
  after local midnight at UTC (26h at UTC+14) on the day they turn 18. Refusing an adult
  briefly is a nuisance; admitting a minor is the failure this gate exists to prevent.
- **The `exactly 18 today is an ADULT` contract survives** — "today" now means today
  everywhere. Every vector was re-anchored to **12:00Z** (the instant at which the AoE day
  equals the UTC day); a bare `Date.UTC(y,m,d)` is midnight, which is still the previous day
  at UTC−12, so unshifted vectors would silently test the wrong day.

### Why the two stated blockers dissolved

Both were checked against the **live catalog**, not the migration files — this database has
twice turned out not to match them.

| Stated blocker | What the live database says |
|---|---|
| "Fixing only the JS side re-opens the JS↔SQL disagreement" | **`over_18` is consumed by nothing.** Zero policies, views, constraints or other functions reference it; `set_over_18()` is the only object that mentions it, and it only *writes* it. And it is never the decider when a usable DOB exists — both consumers read `fromDob !== null ? fromDob : over_18 === false`, and when the DOB is null the trigger writes NULL too, so neither side has an opinion. There is no second gate to disagree with. |
| "(b) member-local day is the correct fix" | It needs `client_profiles.timezone` (read by `shape_user_tz(uid)`) — that table holds **ZERO rows**, and every other `tz` column is 0/0. Every account would fall through to the margin anyway, at the cost of a second table read on the middleware hot path. |

**So the jurisdictional question is deferred, not answered** — the margin is safe under any
answer to it, because it never declares adulthood before the member's local birthday in any
zone. Revisit the member-local day when `client_profiles.timezone` is actually populated;
until then it is a refinement, not a correctness gap.

**The merge gate remains two steps, both on the final head:** CI green and Codex clean. A
clean Codex review leaves **no review record** — it appears as an issue comment or a 👍, so
absence of a record is not absence of a review; a record with a finding is the failing case.

⚠ **Process note for the next session:** the docs commit `3f1ad05d4` landed *after* the
Codex trigger, so Codex's review is anchored to `53cdd22bf` while the head has moved. The
delta is **docs-only**, so the code review is materially valid — but sequence records
commits *before* triggering a paid review next time, so the gate is met by the letter as
well as the substance.

## Why Codex was re-triggered rather than merging

`src/lib/age-derive.mjs` is the one piece of security-relevant logic in this PR that
**no reviewer other than me has ever seen**. It landed in `393efcb99` *after* the last
Codex round, and the eighth self-review then found a **P1 inside it**. That is the whole
argument: the late-added file was unreviewed surface, and it contained the worst defect
of the wave.

## What round 8 fixed (`53cdd22bf`)

**P1 — the age gate admitted a real minor for one day, and the two gates disagreed
about them.** `isMinorFromDob` derived its adult cutoff with
`Date.UTC(year - 18, month, day)`, which **rolls** an impossible anniversary forward
(Feb 29 → Mar 1), while Postgres `date - interval '18 years'` **clamps** it back
(Feb 29 → Feb 28). So on **Feb 29 of a leap year**, a member born exactly 18 years
earlier on **Mar 1** — 17 years and 364 days old — read as an **ADULT** in JS and a
**MINOR** to the `set_over_18()` trigger.

That is the unsafe direction, and it is the two derivations disagreeing about one
person, which is the entire reason the shared module exists. Both cutoffs were verified
against **production Postgres**: `2028-02-29 - interval '18 years'` = `2010-02-28`.
Recurs 2028, 2032, 2036…

The fix takes day 0 of the following month (= the last day of the intended one). Only
February can roll, but the guard is written generally.

⚠ **The 11 original vectors covered a leap-day BIRTHDAY and never a leap-day REFERENCE
DATE** — the one boundary the file was written to protect was the direction it did not
test. The new vector is **mutation-tested**: 12 pass with the clamp, **11 pass / 1 fail**
without it.

**Three comments asserted things the code does not guarantee.** All three are the same
class this wave has now hit eight times — a because-clause written without checking it:

- `age-gate.ts` re-typed the gated-prefix list as **five** prefixes.
  `GATED_API_PREFIXES` holds **seven**; `/api/conversations` and `/api/messages` are
  gated in reality, so a reader auditing coverage from that header concluded **chat was
  ungated when it is gated**. The header now **points at the constant instead of copying
  it** — the copy is what went stale.
- `warroom.ts` carried coverage counts derived from that same stale list (46 inside /
  111 outside). **Re-measured: 157 `/api` routes, 48 inside, 109 outside.** The
  authenticated-outside figure is now marked **DERIVED, not re-measured** (~75, was 77),
  so the next reader does not quote a number nobody checked.
- `middleware.ts` — the line that actually refuses the request — claimed `over_18`
  "cannot be self-asserted, so an explicit false is a proven minor" **unqualified**: the
  one site of five that did not name the freeze migration its guarantee depends on. It
  also described the `over_18`-only mechanism, which is no longer how the value derives.

**Churn.** `middleware.ts` restored to **CRLF**, which is how `main` stores it. A
whole-file LF normalisation had turned a 7-line change into a **596-line diff**
(301+/295−); reviewers, Codex included, read 596 lines of noise to find seven, and it
guaranteed a conflict with any other branch touching the file. Now **14+/1−**; whole PR
down from 1359/488 to **1106/194**.

⚠ **`src/lib/supabase/middleware.ts` is CRLF at rest.** 11 of 238 `.ts` files are, and
there is no `.gitattributes`. Do not LF-normalise it. The Edit tool writes CRLF, so
after editing an **LF** file in this repo, normalise back to LF — and after editing
*this* file, keep CRLF. Verify with `tr -cd '\r' < f | wc -c` (`grep -c $'\r'` is
unreliable in this shell — it silently reported 0 CR on a file with 295).

## Facts a new session needs before touching this area

- **`GATED_API_PREFIXES` has SEVEN entries** — `/api/client`, `/api/nutrition`,
  `/api/ai`, `/api/insights`, `/api/calendar`, `/api/conversations`, `/api/messages`.
  **Read the constant in `src/lib/supabase/middleware.ts`; never trust a prose copy.**
- **`refuseKnownMinor()` has exactly ONE caller** today: `src/app/api/radio/station/route.ts`.
- **`computeMembership` destructures `{ data }` and ignores `error`.** The Supabase
  client **resolves** rather than throws on query errors, so a faulted read yields
  `isMember:false` and exits via **402**, not 503. `refuseKnownMinor` by contrast reads
  `error` explicitly and **fails OPEN** by documented choice (it is a policy control, not
  a security boundary, and there is no second layer behind it).
- **`age-derive.mjs` is `.mjs` + a hand-written `.d.ts`** because `node --test` cannot
  import TypeScript. It imports nothing, which is what keeps it safe inside the edge
  proxy's import chain — `next build` listing `ƒ Proxy (Middleware)` is the proof it
  still bundles.
- **`node --test` prints `ℹ pass` / `ℹ fail`, not `# pass`.** A grep for `^# (pass|fail)`
  matches nothing and makes a mutation test look like it ran when it did not. This
  happened here and was caught only by re-running.
- **Node on Windows cannot read MSYS paths.** `/tmp/x` written by bash resolves to
  `C:\tmp\x` in Node and ENOENTs. `cd` into the scratchpad and use **relative**
  filenames.

## Open — owner / counsel, not engineering

- `privacy@` and `safety@theshapecommunity.com` aliases must exist before ship.
- The backup **retention window** is deliberately unpublished (would not state an
  unverified number).
- The **"within one business day"** reply promise needs an owner decision.
- ⚠ **The DOB-completion flow.** `over_18` is NULL for every account created before the
  DOB gate, so **the age gate refuses nobody among the existing base.** The gate is real
  for new signups only. This needs an owner ruling, not a patch.
- Every legal doc remains **DRAFT pending counsel**.

## Registered, not built

- The remaining authenticated routes needing `refuseKnownMinor()` — **48 of 157 `/api`
  routes are inside the gate, 109 are outside**, and the authenticated-outside count
  (~75) is derived, not measured. **Re-measure before quoting it.**
- **RLS-level age enforcement.** The route layer cannot cover direct Supabase-backed app
  features; a minor reaching PostgREST with the publishable key bypasses every route gate.
- A **subscriber-scoped stream token or proxy** for Radio.
- The non-member **"broadcast coming soon" → join-prompt** copy (owner copy call).
- `sitemap.xml` still advertises legacy pages.

## Standing rules in force on this branch

- Verify before asserting; if it cannot be verified, say so rather than softening it.
- **Explicit paths on every commit** — never `git commit -a`, never bare `git add .`.
- Flag anything needing counsel instead of drafting it.
- **Design hook: no suppression.** Classify findings instead. Measured this round: all
  22 findings are **net zero** between `origin/main` and head, and `radio.html`'s font
  count went 9 → 8 (the branch *removed* one). They resolve to three owner-set decisions
  — brand type stack, the `.nav-radio-air` ON AIR lamp, accent spines.
- **Codex is the gating reviewer; CodeRabbit is never a blocker.**
- Review quota is owner-paid and finite: audit locally first, batch fixes into **one**
  push, re-trigger rarely.

## The pattern worth carrying out of this wave

Eight rounds, and **every round from 5 onward found a defect adjacent to or caused by
the preceding fix.** Round 8 is the sharpest case: severity went *up* (P2s → a P1)
because round 7 added a **new file**, and that file's only review was mine.

The generalisation: a flat findings curve says *change approach*; a curve that
**rises** after a late addition says the addition itself is the unreviewed surface.
Treat code written to perform a fix as new surface needing independent review — not as
the fix.
