# Handoff — 2026-08-17 · the legal/age-gate merge + the check-in engine

Read this alongside `docs/WORKLOG.md` (the two 2026-08-17 entries and the 08-16 ones
below them). Everything here is verified state unless it says otherwise.

⚠ **This is the day two long-running waves closed.** PR #1888 — open since 2026-08-15 and
the subject of [`docs/HANDOFF-2026-08-16b.md`](HANDOFF-2026-08-16b.md) — merged, and the
check-in engine shipped three of its four phases. **Nothing is in flight.**

## State snapshot

| | |
|---|---|
| `main` | **`7b087c203`** — this handoff's own refresh (#1900), on top of the records merge `103177d17` (#1899). The wave code landed at `ba0449c2b`. |
| In flight | **NOTHING** — and the proof is the **merged SHA** each PR landed as (row below), not a branch diff. ⚠ Do NOT try to re-prove it with `git diff --stat origin/main <branch>`: a two-dot diff reports the NET difference in **both** directions, so it cannot tell *behind `main`* from *carrying unmerged work* — `claude/radio-legal-gates` shows 116 insertions across 69 files, and every one is a stale older line. Squash merges also make `git cherry` and three-dot diffs read as "unmerged". |
| Shipped today | **#1888** → `2f6c38b6c` · **#1893** → `520630e33` · **#1894** → `5268ef80c` · **#1895** → `ba0449c2b` (a real merge commit, not a squash) · **#1896** → `915c20a0a` · **#1897** → `249c6c6f3` · **#1898** → `d5759521c` · **#1899** → `103177d17` (these records) |
| War Room | A **Check-in engine** section now leads the checklist (`src/lib/warroom.ts`) — what shipped, plus the residuals below as `pending`. #1888 updated its own War Room items when it merged, so the legal/age-gate side needed no change. |
| Suite | **1927/1927**, re-measured at `3b33c1d8d`. ⚠ This wave recorded **1855/1855**, and **no file under `tests/` has changed since `ba0449c2b`** — so the two cannot both be right. Green either way; **re-run `npm test` rather than quoting either figure.** |
| Migrations | **Zero owed.** The check-in wave needed none — every field already existed on `daily_health_snapshot` / `user_goals`. #1888's two (`2026-08-15-profiles-dob-immutable.sql`, `2026-08-16-created-at-freeze-and-application-dob.sql`) were applied + **behaviourally** verified on production before the merge. |
| Branches | All **kept** per house rule. `claude/radio-legal-gates` (worktree `C:/Users/cperr/shape-legalgate-wt`), `claude/checkin-{engine-spec,optional,vitals-engine,casefile,readout}`, `claude/cook-{mode-ux,step-ingredients}` — all now behind `main`. |

## What shipped

**#1888 — legal pages · Radio licensing · privacy mechanisms · the 18+ age gate.** Closed at
Codex round 22 plus a self-review of it. The round-by-round record, the honest limits and the
counsel items live in [`HANDOFF-2026-08-16b.md`](HANDOFF-2026-08-16b.md) and are **unchanged
by the merge** — read that file before touching anything in this area.

**The check-in engine** (spec `docs/superpowers/specs/2026-08-17-checkin-engine-design.md`):
**§A** the engine's vitals leg + three absence-gated rules + the widened roster read (#1896),
**§B** the coach Case File reading the same vitals (#1897), **§D** the optional daily check-in
(#1895). **§C — the client weekly readout — is NOT built.**

## ⚠ Things that will bite you if you don't know them

- **A read that can't know is not a read that says "no".** `getUserGoals` resolves **NULL**
  for every can't-know case (no backend, not signed in, query error — it never rejects) and
  `{}` for a genuinely absent row. Flattening the two turned a failed read into "no
  preferences" and converged a member's OFF setting back to ON. The same class killed the
  wave's last defect: **`maybeSingle()` RESOLVES `{data:null,error}`** — it does not throw —
  so a failed profile read looked identical to an absent row, took the create path, and the
  upsert **overwrote** the existing row.
- **`saveUserGoals` UPSERTS the whole document.** Any whole-doc save issued before a pane's
  hydrate lands publishes that pane's DEFAULTS over everything else in the doc — units,
  privacy, meal times, phases. The guard belongs at the **writer**, not on the row a review
  happens to name.
- **`.slice(-7)` is not "the last 7 days".** `/api/client/progress` returns up to 400
  chronological snapshots with **no recency filter**. Anything reading a tail of that array is
  reading observations, not a window. ⚠ **`sleepRecoveryFromProgress` still does this** — it
  is registered, not fixed, because changing it alters shipped `sleep_low` firing.
- **A future-dated row is served forever by a floor-only window.** `/api/client/checkin` takes
  the day from the **request**, so `2099-01-01` is writable. The vitals window was only one of
  four victims — the same row was also `latest` sleep, the current RESTED rating and a
  readiness input. The ceiling is **tomorrow**, not today: `snapshot_date` is the member's
  LOCAL day, so a member ahead of UTC legitimately writes one. ⚠ **The ceiling shipped on the
  COACH path only** (`/api/clients/[id]/shared-overview` + `bsVitalsLeg`). The member path
  still has a floor only — see open item 4.
- **Severity is computed before routing tags it.** `flags.length >= 2` → red runs **before**
  `readOnlyFlags` marks a non-owner's flag, so a nutrition-owned flag pushed to every role
  turned a **trainer** red. Route the flag out of the discipline that doesn't own it, don't
  patch the colour.
- **`/auth/callback` is not signup-only.** Password resets, magic links and email-change
  confirmations all exchange a code there carrying **SIGNUP metadata**. Provision fields only
  when the row lacks them.
- **`profiles.role` is NOT NULL with no default** (`email` is nullable — read the LIVE
  catalog, not the migration files). An INSERT omitting `role` fails 23502, and under the
  absence-refuses age policy a failed provisioning write is a **lockout**, not a retry.
- **Commit before mutation-testing.** Round 20's guard rewrite was reverted by a
  `git checkout --` while still uncommitted; the commit then reported *"1 file changed, 1
  insertion(+)"* while its message described changes that weren't in the diff. **Read the diff
  back.**
- ⚠ **A STALE `public/newdesign/nd/` BLOCKS EVERY CODE COMMIT IN THIS WORKING TREE.** That
  directory is the **deploy-time precompile output** — gitignored, untracked, absent from
  `main`, and regenerated by `node scripts/build-newdesign.mjs` in Vercel's `buildCommand`. If
  a local copy exists (this tree had 246 files last built 2026-08-01), the pre-commit hook's
  `tests/provider-apply-dob.test.mjs` scans it, finds the **compiled** `nd/signup.js` posting
  to `/api/apply`, and fails *"no unregistered surface posts to /api/apply"* — even though the
  **source** `public/newdesign/signup.jsx` is registered. CI never sees it, so `main` stays
  green and the failure looks like a real defect in whatever you were committing. **It is not
  your change.** `rm -rf public/newdesign/nd` and re-run. ⚠ The guard arguably should exclude
  its own build output — **registered, not fixed** (it was a records PR).
- **A records PR inherits the claims of the commits it summarises.** Both Codex findings on
  #1899 were over-claims that came from the shipping commit messages and a `selfRecord` code
  comment, which I repeated verbatim. **Verify the mechanism against the code before writing
  it into the record**, exactly as you would for the code itself.

## What is actually open

1. **OWNER on-device passes** (the standing gate on all three of today's UI-touching PRs):
   - **#1897** — a coach opening the Case File for a **device-synced**, a **vitals-only** and
     a **rating-only** member: no fabricated `0/10` gauge, no contradictory redaction line in
     any of the three.
   - **#1895** — toggle **Daily check-in** off: the home CHECK-IN DUE bulletin and the logged
     residue vanish, the quiet **Check-in** index row still opens the page and the hydration
     quick-add; toggling back on restores the bulletin. Then the same across papers.
   - **#1894** — the Terrain ridge's next-tier label clear of the summit flag, **in landscape
     and at native `100vw`**, which is where the first fix failed.
2. ⚠ **The Codex gate was WAIVED on #1897** (owner-ruled, PR-scoped). Round 3's two
   chokepoint moves — the future-date drop at the route and the single derived presence model
   — were therefore **never independently confirmed**. The standing gate (CI green **and**
   Codex clean on the final head) is unchanged on every other PR.
3. **§C — the client weekly readout is unbuilt.** `POST /api/ai/weekly-readout` is orphaned:
   no entry point. ⚠ Wiring it requires `SNAPSHOT_FIELDS` **and** `CORRELATION_PAIRS` /
   `SnapshotPoint` extended in the **same** PR — `computeCorrelations()` only processes the
   pair catalog, so extending the fields alone makes both the model evidence and the
   deterministic fallback silently omit them. The cost bound (one model call per member per
   ISO week) must be enforced **server-side** under an atomic per-member claim, not in the UI.
4. ⚠ **TWO RESIDUALS CODEX FOUND IN THE RECORDS THEMSELVES** (#1899 — merged with the claims
   narrowed and both threads answered; **neither residual is fixed**). Both verified against
   the code, both real, and both were over-claimed by the shipping commit messages and by a
   code comment before they were over-claimed here:
   - **The future-date ceiling is COACH-ONLY.** `/api/client/progress` returns every snapshot
     and `vitalsFromProgress` (`signalsMap.mjs:161`) compares `p.date >= cutoff` with **no
     ceiling**, so a `2099-01-01` row still becomes member engine input and can be persisted
     into `notify_snapshot`; the unbounded sleep/readiness consumers still see it too.
   - **Opting out does not clear the persisted snapshot.** `bsDailyCheckinApply` writes the
     per-uid mirror and dispatches `shape:checkinPref` — nothing else. The stored
     `notify_snapshot` changes only at the next evaluation, and `/api/ai/notify/cron` reads
     those rows **without consulting `client_settings.dailyCheckin`**, so a member who opts
     out and never reopens the app can keep drawing vitals-derived notifications.
     ⚠ `selfRecord`'s own comment says it goes quiet "at once" —
     **`mobile-app/src/services/shapeSignals.js:81-83`**, where the clause "and the persisted
     `notify_snapshot`" is the false half (the client leg really does drop immediately).
     **Fix the comment with the code.**
5. **Registered, not built:** the `sleepRecoveryFromProgress` window (above) ·
   `avgHydrationL` is computed **server-side** at
   `src/app/api/nutritionist/analytics/route.ts:196` (typed at `:147`) and **nothing renders
   it** — `dashBusiness.jsx`'s `DbzOutcomesZone` (`:338`) is the consumer that drops it, and
   ⚠ that file contains **zero** hydration references, so grepping it for `avgHydrationL`
   finds nothing and reads as already-fixed. Render it or delete the compute ·
   energy/hunger joining the client Progress trend tabs.
6. **Everything in [`HANDOFF-2026-08-16b.md`](HANDOFF-2026-08-16b.md) §"What is actually
   open" items 2–4 survives the merge**, notably: every legal doc is **DRAFT pending
   counsel**, `privacy@` / `safety@theshapecommunity.com` must exist before ship, the legacy
   apply surfaces are verified **statically only** (no browser click-through — house rule),
   the **DOB-completion flow for pre-gate accounts is an owner ruling**, and self-asserted age
   is the honest ceiling.
7. **Housekeeping:** the main checkout carries untracked `.impeccable/` and
   `public/.impeccable/` directories (tooling output, deliberately not committed). The stale
   `public/newdesign/nd/` precompile output was **deleted** on 2026-08-17 to unblock the
   pre-commit guard (see the trap above); it regenerates on demand and at deploy.

## Standing rules in force

- Merge gate is **two steps on the final head**: CI green **and** Codex clean. A clean Codex
  verdict is an **issue comment or a 👍**, never a review record — polling `/pulls/N/reviews`
  waits forever on a PR that already passed.
- Review quota is **owner-paid and finite**: audit locally first, batch fixes into **one**
  push, re-trigger rarely. Every push is a purchase.
- **Explicit paths on every commit** — never `git commit -a`, never a bare `git add .`.
- Branches are **kept** after a squash-merge, never deleted.
- Migrations are **owner-run**: post the raw GitHub link only.
- Flag anything needing counsel instead of drafting it.
