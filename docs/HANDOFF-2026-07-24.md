# Handoff — 2026-07-24 · Coach box + profile customization: BUILD complete, all migrations applied

**State:** the profile-customization wave is **shipped end to end and fully
closed**. Box A/B → profile PR C (member M1–M4) → **PR D #1821** (coach P1–P5,
`main` `fc6b9e12`) → **PR E #1822** (member M5 film, `main` `322822b2`), all
merged, plus WORKLOG doc PR **#1823** refreshing the auto-loaded "Latest"
pointer. **All THREE owner migrations are APPLIED + verified live** — nothing
is left owner-side on this wave. Continuation of
[`docs/HANDOFF-2026-07-23.md`](HANDOFF-2026-07-23.md) (the spec phase); the
build gate ("pause before you start build") was lifted by the owner's
"continue" + "merge when clean and continue build."

## Migrations — all live (verified read-only against prod 2026-07-24)

| File | PR | State on prod |
| --- | --- | --- |
| `2026-07-23-provider-listing-media.sql` | box PR A | `listing_media` jsonb present on **`trainers`** + **`nutritionists`** |
| `2026-07-23-coach-reviews-owner-id.sql` | profile PR D | `coach_reviews.owner_id` column **+ index** present; SECURITY DEFINER stamping trigger live |
| `2026-07-23-member-films-bucket.sql` | profile PR E | `member-films` bucket present — public, **60 MB**, video-mimes-only (`mp4/quicktime/webm/x-m4v/m4v`), **4** owner-folder RLS policies |

## What shipped

- **PR D #1821 — coach Signal profile (P1–P5):** intro film · the Line ·
  prompts · business card · the **Wins wall** (pins REAL `coach_reviews`,
  resolved by id + the immutable **`coach_reviews.owner_id`**). 10 review
  rounds; the load-bearing catches were the owner_id trust boundary and an
  account-delete break its immutability introduced, both Codex-validated.
- **PR E #1822 — member M5 intro film:** a dedicated video-only **`member-films`**
  bucket + its own **`filmMember`** doc key (so coach `film` and member
  `filmMember` coexist rather than overwriting one shared key — the round-6→9
  saga). 11 review rounds. A **pre-push adversarial review caught a Critical**
  (member film serialized only in the coach spread → never persisted) before
  the first push; post-open review then hardened the account-deletion purge
  end to end (bucket coverage · >1000-object pagination · sub-folder recursion ·
  storage-purge-FIRST with abort-on-failure · a post-delete sweep of the
  writable window).

## Load-bearing designs (don't re-litigate)

- **`coach_reviews.owner_id`** is server-derived by a SECURITY DEFINER trigger
  from the slug (non-forgeable, CWE-639), stamped on insert, **immutable for an
  identity-stable edit**, **re-derived on a slug/kind repoint**, and it **honors
  the FK `on delete set null`** (a NULL clear passes through so account deletion
  works). **NO backfill** — every heuristic mis-attaches after a slug
  rename/reuse, so all pre-migration rows stay NULL forever (write-path stamping
  only).
- **Film keys are PER-ROLE:** coach P1 → `profile_custom.film` (coach-media),
  member M5 → `filmMember` (member-films). The normalizer takes `opts.filmKey`.
- **`member-films`** is a DEDICATED video-only bucket, NOT a `community-photos`
  widening (photo paths don't check `file.type`).
- **Account-delete purge** (`/api/account/delete`) is robust end to end: every
  user bucket (incl. the film buckets) with re-list-from-start (no >1000 skip),
  sub-folder recursion, interleaved-page pagination, storage-purge-FIRST with
  abort-on-failure, and a post-delete sweep of the writable window. Deliberately
  service-role (documented admin-erasure exception).

## Open follow-ons (registered, NOT started — declined-with-rationale in review)

- **Platform-wide media orphan-GC** — remove/replace leaves the old object in a
  public bucket; same ruling as PR C, now scoped to include the film buckets.
- **Persisted provider-slug column** — the owner_id trigger resolves the coach
  by scanning provider rows; a stored slug would replace the scan.
- Re-key the general **Reviews section** onto `coach_reviews.owner_id` (the wall
  already uses it; the section stays slug-keyed for now).
- **On-device pass** across the new profile surfaces + P3 render verification on
  Signal/Terrain.

## Process wins recorded

- The PR E pre-push adversarial review paying off (the never-persisted-film
  Critical) — the pre-push-self-review rule.
- Per-role film keys resolved the coexistence cleanly (zero-migration since
  member films are brand-new).
