# Session handoff — 2026-07-13

Two waves shipped end-to-end in one session: **the share card** (#1692 spec ·
#1693 build) and **the full Work domain** (#1694 spec · #1696 / #1697 / #1698
builds). All merged to `main`; every migration applied + verified live;
suite **620**. Read `docs/WORKLOG.md` (dated 2026-07-13 entries) alongside
this file.

## State snapshot

- `main` clean; the session's last merge is PR C (`a52d75ff`) + this docs PR.
- Branches kept + re-synced to main (house rule): `claude/share-card-build` ·
  `claude/work-domain-spec` · `claude/work-domain-a` / `-b` / `-c` ·
  `claude/worklog-1693-review-round`.
- `npm test` = **620** via glob discovery — the script is now
  `node --test "tests/**/*.test.mjs"` (the bare `tests/` directory form
  FAILS on Windows: "Cannot find module …\tests" — don't "simplify" it back).
- Migrations applied + verified live this session (all idempotent):
  1. `2026-07-13-habit-domain.sql` — nullable `user_habits.domain`
     CHECK `in ('work')`.
  2. `2026-07-13-work-milestone-points.sql` — **run twice** (the review
     round revised it): `career` ledger category · `award_work_milestone`
     (+25, month bucketed from the POST's `created_at` in member tz,
     `granted:false` no-op on a same-month duplicate, anon=f/authd=t) ·
     `award_community_post` milestone exclusion.

## What shipped

### The share card (#1692 spec · #1693 build · #1695 worklog amendment)

A member's OWN activity renders as a canvas-drawn **1080×1920** fixed-dark
brand PNG (workout/run with the real route polyline · PR delta · meal THE
PLATE) fired through the OS share sheet AS A FILE → Instagram offers Stories.
Desktop downloads the PNG; abort is silent; nothing leaves the device beyond
the share sheet's file. Review round added: the **Session details page** as a
share entry point (ONE shared `BSShareChooser` for card + detail — never two
implementations) and the `bsWrapText` oversized-token ellipsis.

### The Work domain (#1694 spec · #1696 · #1697 · #1698)

Owner-directed during the brainstorm: a work goal area, **milestone Shape
Score points**, and website parity on every PR. Standing guardrails baked
into the spec: share-by-choice · **no compensation figures anywhere ever**
(money is this domain's calories) · honest-absent · never-shaming ·
rarity-capped scoring.

- **PR A #1696 — work habits + THE WORK station.** Add-habit sheet WORK
  chips + a Work-habit toggle stamp `domain:'work'` (explicit, never
  name-inferred; same +3). WORK tags (`t.BLUE` mobile / `#7aa7dc` web) on
  the Habit Ledger, home slate, and website Habits page. THE WORK station
  joins The Contract (workMeta + `work[]` targets, Career/Skills/Projects
  templates, the existing share toggle; shared work goals render on the
  coach Case File + web client page). Web `goalsFromDoc` folds `doc.work`.
- **PR B #1697 — THE APPOINTMENTS.** Milestone composer BOTH surfaces:
  stamp picker (six canonical tokens — keep `BS_MILESTONE_STAMPS` /
  `DC_MILESTONE_STAMPS` / the SQL allowlist in sync, they cross-reference
  each other), headline ≤80, detail ≤140 (omitted when blank). Card block
  both surfaces; `career` reaction bucket → **"Onward"** (exact
  `activity_type 'milestone'` token); Milestones chip (app) / MILESTONES tab
  (web); legend + The Record wiring on both twins. **The +25 claim is
  AWAITED with a localStorage catch-up on BOTH surfaces**
  (`shape.careerAwardPending`) — it can never be permanently lost; the chip
  shows ONLY on `granted:true`.
- **PR C #1698 — THE CROSSOVER.** `crossoverRead` + `crossoverCopy` live in
  **`dashSignals.js`** (ONE implementation: web script · mobile via
  `window.DashSignals` · Node tests — the spec's `crossover.mjs` deviation
  is deliberate). Exact statistic: `p = Σdone/Σscheduled` per side; fires
  only at |gap| ≥ 12pp AND ≥ 1.65·SE (two-proportion, n = scheduled days per
  side); sleep bands <6.5h vs ≥7h with **[6.5, 7) excluded**; floors span
  ≥ 21d + ≥ 8 days per side — below any of it NOTHING renders. Mobile
  `BSCrossoverCard` (Progress Overall) + a conditional web Progress widget.

## Architecture you'll need next time

- **Habit domain**: rides `user_habits.domain` + the habit encode/decode +
  `_bsMapServerHabits`; the habits GET reads `select('*')` (migration-safe)
  and the create retries without the column on `42703`/`PGRST204`.
- **Native-safe reads**: components must NEVER raw-`fetch` a same-origin
  `/api/*` path (dies on native builds) — use the shapeBackend helpers;
  `window.ShapeHabitsData.list()` exists for habits.
- **Milestone posts**: `community_posts.metrics {kind:'milestone', stamp,
  detail}` + `activity_type 'milestone'`; the composer sets `skipAward`
  (client) AND the RPC guard excludes milestones (server) — no +5 ever.
- **Award pattern**: `window.ShapeCareerAward.claim(postId)` awaited at post
  time; `catchUp()` fires on session resolve (mobile) / Community page load
  (web).
- **Crossover copy**: change wording ONLY in `crossoverCopy` (dashSignals) —
  both surfaces render its rows verbatim; a sleep-only read spells out its
  subject.
- **i18n**: the new `home:tag.work` key falls back to English pending the
  paused i18n rollout (Profile is the next surface when it resumes).

## Open follow-ups

- **OWNER on-device pass ×2**: (1) share card → Share as image → Instagram
  Stories; (2) the work domain — work chips → WORK tags · THE WORK station ·
  a milestone post → "Onward" + the "+25 · Career" chip (once/month; Just-me
  earns too) · the crossover card lights up after ~3 weeks of work-habit
  data.
- **Shelf**: share card PR B (web parity + native `instagram-stories://`
  deep-link once the iOS build exists) · beat layers L/M (owner pick) ·
  i18n resume (Profile next) · Instacart owner-side application
  (INSTACART_API_KEY) · trainer sell-a-plan live-Stripe verify · security
  P2s (per-endpoint paid enforcement · supabase-js SRI self-host).
- **Watch for**: CI "Set up job" runner flakes (both jobs die instantly —
  `rerun-failed-jobs`, don't debug code); the `node --test tests/` Windows
  trap above.
