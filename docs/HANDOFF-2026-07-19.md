# Handoff — 2026-07-19 · The buildable wave: specs + plans written (Fable), builds queued (Opus)

## State snapshot

- `main` at **`fe23a6e0`** (plans #1767) ← `54a3e6bf` (specs #1766) ← `7858d035`
  (#1765 War Room flip). Branch `claude/i18n-coach` tracks main; **PR #1768**
  (docs: plan fold-in + this session's journal docs) is the only thing in
  flight — it runs the FULL CodeRabbit wait (see the process correction below).
- **Nothing from the wave is built.** The write half is done; the build half
  runs on **Opus** ("basically i want to write everything on fable and then
  build everything with opus" — owner, binding).

## What shipped

- **#1765** — the #1764 `user_activity_live` migration verified live
  (RLS + both policies byte-correct · `private` unrepresentable · 0 ERROR
  advisors) + the stale War Room OWNER item flipped.
- **#1766 — six specs** (`docs/superpowers/specs/2026-07-19-*`), 3 CodeRabbit
  rounds / 45 findings folded in. The Cycle (flagship) · live-progress web ·
  variance band · live coach channel (**owner-ratified** coach-link gate) ·
  live cooking detail · nora_sets schedule.
- **#1767 — nine plans** (`docs/superpowers/plans/2026-07-19-*`), full
  writing-plans standard, its 25-finding round folded in. The MERGED PLANS
  ARE THE BUILD CONTRACTS — execute verbatim.
- **#1768 (in flight)** — the late post-merge #1766 round (3 findings) +
  the #1768 round itself (2: NaN-safe expiry guards `!(expMs > 0)`,
  data-boundary RLS assertions) + WORKLOG/War Room/this handoff.

## ⚠ Process correction (owner-flagged — binding)

I merged #1766/#1767 after addressing findings + CI green but **before
CodeRabbit's re-review of the fix commits completed** (over-read the owner's
"reviews done" nudges); a fifth #1766 round landed 5 minutes post-merge.
**The rule now, no inferred exceptions:** address every round → push → WAIT
for the re-review of the fix commit (edited-in-place summary comment + a
quiet period) → then merge. Also proven this session: **gitleaks IS a
required check** (merge 405s while it runs) — the WORKLOG's "advisory" claim
was stale and is now corrected.

## The build queue (next session, on Opus)

One plan per PR, merged before the next starts, full gate each time:

1. `2026-07-19-live-progress-web.md` — FIRST (canonical `liveProgress.mjs`
   split-with-shim is the foundation for 3 and 4). No migration.
2. `2026-07-19-variance-band.md` — any time. ⚠ migration.
3. `2026-07-19-live-cooking-detail.md` — after 1. ⚠ migration (expiry-RLS).
4. `2026-07-19-live-coach-channel.md` — after 1. ⚠ migration.
5. `2026-07-19-nora-sets-schedule.md` — any time. ⚠ migration.
6. `2026-07-19-cycle-a-engine.md` — ⚠ migration (cycle-events).
7. `2026-07-19-cycle-b-member-surfaces.md` — i18n ×13; the disclaimer key is
   PRIORITY for the standing human translation review.
8. `2026-07-19-cycle-c-coach-share.md`.
9. `2026-07-19-cycle-d-website-parity.md` — closes the wave.

**5 OWNER migrations** land with their build PRs (raw link only, per
convention): cycle-events · roster-weekly-adherence · user-activity-live-coach
· user-activity-live-expiry-rls · nora-sets.

## Architecture you'll need (locked in by the plans)

- **Canonical split-with-shim:** shared pure modules live in
  `public/newdesign/*.mjs` (web loads native ES module → `window.<Ns>Lib`,
  mobile imports via a re-export shim in `mobile-app/src/services/` that
  keeps writer-side deps like `workoutShare.mjs`); a referential-equality
  test proves one implementation. Web namespaces: `ShapeLiveValidate` ·
  `ShapeVariance` · `ShapeSetsLib` · `ShapeCycleLib` (mobile data layers
  keep their own `window.Shape*` names — never collide them).
- **GUC-guard triggers** (`shape.cycle_rpc`, the #1707 `shape.adjust_regen`
  pattern) make cycle settings + cycle consent receipts RPC-only at the DB.
- **Coach client pages have NO supabase loader today** — TrainerClient.html /
  NutritionistClient.html gain the SRI'd vendor + `/supabase.js` + module
  tags in build 1 (later builds guard against double-insertion).
- **`ShapeRadioLive.station()` → `{configured}`** is the real-vs-mock stream
  gate (nora-sets LIVE banner).
- Consumer hygiene invariants that recur in every live surface: `evented`
  TOCTOU guard · NaN-safe expiry (`!(expMs > 0)` = absence) · synchronous
  state reset on subject change · re-fetch (not assume) at expiry.

## Open follow-ups

- **#1768** — wait out its re-review, address anything new, merge, re-sync.
- **Owner standing items:** native iOS/Android build (critical path) · the
  on-device pass queue (see War Room) · Dependabot automated security fixes
  (blocked on permission) · the standing human translation review.
- **War Room:** the wave has its own section at the top of the checklist
  (`src/lib/warroom.ts`) — flip build items as PRs merge.
