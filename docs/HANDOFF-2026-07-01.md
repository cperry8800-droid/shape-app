# Handoff — 2026-07-01

Two sessions ran on 2026-07-01. **Session B (latest)** is below; **Session A** (the security audit + remediation) follows under "## State snapshot". *(Session A's `#1481` email-enum PR + its migration have since been completed — the audit is fully closed.)*

## Session B — Web notifications + reminders parity · iOS push prep · coach-marketing docs

### What shipped (all squash-merged to `main`, branches kept, CI green)
- **Reminders parity + Notifications dashboard on the website** (`#1483`, `public/newdesign/clientMeSettings.jsx`):
  - **`ReminderCard`** ports the mobile `BSReminderManager` (weigh-in / check-in / water / progress-photo / custom nudges, time + days) over the existing `/api/client/reminders` (`user_scheduled_reminders`, cookie auth).
  - **`NotificationDashboard`** ports the mobile `BSNotifyPrefs`: master mute, quiet hours, daily cap, the per-type × per-channel matrix (8 types × App/Push/Email), and habit reminders (enable toggles). Wired to the **already-live** notification-center tables (`notification_settings` / `notification_preferences` / `habit_reminders`) via the `get_notification_center()` RPC + RLS upserts through `window.shapeDb.client` — the same tables `src/lib/ai/notify-core.ts` reads. **No new route or migration.** `notification_preferences` stays override-only (toggling back to default DELETEs the row). The old "Privacy & notifications" card is trimmed to "Privacy".
  - CodeRabbit's 4 findings all fixed + resolved (load-failure-vs-empty, stale custom label, write-error surfacing, override-only delete-on-default).
- **iOS native push prep** (`#1484`): `aps-environment` entitlement committed to `mobile-app/ios/App/App/App.entitlements` (the last repo-side gap for the system-push P1) + **`docs/native-ios-build-checklist.md`** (the Mac-side owner sequence: cap sync → App-ID caps → APNs `.p8` into Firebase → `GoogleService-Info.plist` → signing → submit; + Stripe Connect / IAP caveat).
- **War Room cleanup** (`#1484`): 3 stale `pending` items flipped to `done` (#1438/#1439/#1440, shipped 06-27).
- **Coach-marketing funnel docs** (`#1485`): June-1 assets updated to the flat-15%-commission / members-pay-$5 messaging + 3 new campaign docs (awareness → nurture → recruiting). Docs only.

### Architecture you'll need (Session B)
- **Notification-center backend** (already live, `supabase-migrations/2026-06-17-notification-center.sql`): `notification_settings` (muted / quiet_start / quiet_end / tz / daily_cap), `notification_preferences` (per-user × type × channel `inapp|push|email`, **override-only** — a missing row = default `{inapp:true, push:true, email:false}`), `habit_reminders` (per-habit schedule). One-shot read RPC `get_notification_center()` → `{settings, prefs, reminders}`. There is **no `/api/**` route** — both mobile (`window.ShapeNotifyPrefs`) and web go **direct to Supabase** (RLS-scoped); web uses `window.shapeDb.client` (carries the session).
- **Reminders vs habit reminders are separate tables**: `user_scheduled_reminders` (`/api/client/reminders`, standalone nudges) vs `habit_reminders` (per-habit, keyed on `habit_id`). Both ride the notifications→push spine.
- **CodeRabbit "done" detection** (lesson learned this session): read the *current* body of its summary comment (it edits that comment in place: "processing" → walkthrough) + resolved-thread state via GraphQL — the literal "Actionable comments posted:" string misses in-place edits and clean 0-issue reviews. A rate-limit notice (`created == updated`, 0 inline comments, 0 submitted reviews) is NOT a review.

### Open follow-ups (Session B)
1. **System-push P1 finish (Mac-only):** APNs `.p8` upload to Firebase + `GoogleService-Info.plist` + the native iOS build — see `docs/native-ios-build-checklist.md`.
2. **Web habit-reminder authoring:** the web *surfaces + toggles* `habit_reminders`, but setting the per-habit time/days is still mobile-only (no web Habits authoring page).
3. **CodeRabbit org spending cap:** `#1484`/`#1485` merged CI-green but un-reviewed (cap posted a rate-limit notice). Re-trigger with an `@coderabbitai review` comment once the cap resets if a retro review is wanted.

---

## State snapshot
- `main` == the session dev branch, all green. A full read-only **security audit** ran, then a **remediation pass** fixing every P1 + P2. **10 PRs merged** this session; **1 open** (email-enum fix, `#1481`, deliberately held for a login-path staging test).
- Audit report: [`docs/SECURITY-AUDIT-2026-06-30.md`](SECURITY-AUDIT-2026-06-30.md). Remediation ledger: [`docs/REMEDIATION-2026-06-30.md`](REMEDIATION-2026-06-30.md).

## What shipped

### The audit (read-only)
Whole-repo pass — API authz (141 routes), Supabase RLS + SECURITY DEFINER RPCs (deployed-state cross-checked via `pg_proc`/`pg_policies`/`has_function_privilege`), secrets, payments/integrations, plus dead-code / inefficiency / consolidation / deps. **Headline: strong posture, two real P1s.** Root cause of the security findings: Supabase auto-grants `EXECUTE` on public functions to `anon`+`authenticated`, and a set of admin/cron/catalogue SECDEF RPCs were never revoked (several also used `auth.uid() IS NULL` as a "service role" proxy that `anon` satisfies). No exposed secrets, no XSS, `npm audit` clean.

### The remediation (one PR per finding — all merged unless noted)
- **#1471 AUTHZ-P2-oauth-redirect** — `safeReturnPath()` guard on the integrations OAuth `return` (+ CodeRabbit's control-char follow-up) + test.
- **#1472 AUTHZ-P2-console-idor** — `is_coach_on_client` route gate + RLS split (`coach_focus_banners`/`coach_pushed_items`).
- **#1473 AUTHZ-P2-program-idor** — `unauthorizedAssignTargets` gate + RLS split (`coach_program_assignments`).
- **#1474 AUTHZ-P1-fulfillment-pii + 4×P2** — revoke `admin_list_store_fulfillment`/`admin_mark_store_fulfilled`/`consume_store_credit` from anon/authenticated; anon-reject on `set_metric_source`/`set_program_detail`.
- **#1475 AUTHZ-P1-store-credit + P2** — `store_catalogue` table; `redeem_store_item`/`redeem_store_order` now ignore client-supplied cost/credit.
- **#1476 AUTHZ-P2-claim-jack** — `claim_provider_row` → 3-arg service-role-only + route via `createAdminClient`.
- **`#1481` AUTHZ-P2-email-enum (OPEN)** — `get_email_for_username` revoked to service_role; username login now goes through the rate-limited `POST /api/auth/resolve-username` (mobile `shapeBackend.js` + web `login.jsx`).

### Dependabot (all handled)
`#1477`/`#1478` (Actions bumps) + `#1479` (mobile deps) merged clean. `#1480` (web deps: `stripe 22.0→22.3`, `@supabase/ssr 0.10→0.12`, react patch) broke `tsc`; fixed the two compat errors (Stripe `apiVersion` → `2026-06-24.dahlia`; `basePayload` typed `Record<string, unknown>` for the tightened supabase insert types) and merged.

## Migrations — status
All 6 audit migrations **applied on Supabase + verified live** (grants, RLS policies, `store_catalogue` = 19 rows, function bodies). The email-enum migration (`2026-07-01-get-email-for-username-service-role.sql`) is **NOT yet applied** — apply it WITH the `#1481` deploy.

## Open follow-ups
1. **`#1481`** — review + click through **username login** on staging (mobile + web), then merge and apply its migration together. Pre-launch, so no shipped native installs to break.
2. Nothing else outstanding from the audit — every other P1/P2/P3 is either fixed or recorded in the report's fix queue.

## Architecture you'll need
- **Verify SECDEF grants** after any RPC change: `has_function_privilege('anon'|'authenticated', '<fn>(<args>)'::regprocedure, 'EXECUTE')`.
- **The revoke gotcha** (WORKLOG #1459): a bare `revoke … from public` leaves the `anon`/`authenticated` grants — always `revoke … from public, anon, authenticated` and re-`grant … to service_role`.
- **RLS write-scope pattern** (`2026-06-17-coach-write-scope.sql`, reused by #1472/#1473): split a broad `FOR ALL` policy into owner-scoped SELECT/UPDATE/DELETE + a stricter INSERT gated on `is_discipline_coach_on_client`.
- **Store pricing is now DB-authoritative** — keep `store_catalogue` in sync with `src/lib/store-catalogue.ts`.
