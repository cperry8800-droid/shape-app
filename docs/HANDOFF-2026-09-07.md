# Session handoff — 2026-09-07

## 0. READ THIS FIRST

⚠ **UPDATED 2026-09-07, SECOND SESSION — THE TWO BRANCHES BELOW ARE CONSOLIDATED ONTO
`claude/handoff-docs-review-mj1oxt`, AND THE STALE ROWS IN §1 ARE RE-MEASURED.** Everything
this file says about *what was done* stands. What follows is what changed underneath it, each
point measured rather than recalled:

- **Both branches are now one.** `claude/handoff-docs-review-mj1oxt` = `origin/main`
  (`da2c8ad`, the #2011 merge) + the five launch-cut commits (`926ba40` `cf884db` `cfea985`
  `acd7a7c` `fb14171`) + `ec0b8a7` cherry-picked from the worklog-review branch. Its other
  commit, `0039525`, was **not** taken: it is the orb record's older copy, which `acd7a7c` had
  already brought across and amended (the v7.2 header; `BPM` derived rather than typed). The
  launch-cut copy is the superset — checked by diff, not assumed.
- ⚠ **`claude/worklog-review-ozn07z` WAS NOT RESET ONTO THE #2011 MERGE, whatever the table
  below says.** Measured: `ahead 2 / behind 1` — it sat on `66cf547` (#2010, the archive
  split), one commit short of `da2c8ad`. The commit taken from it applied cleanly because it
  only ADDS files. The claim was wrong; the work was not. *A branch's base is measured with
  `rev-list --count`, never recalled.*
- **The suite IS re-run now** — §1 carries the fresh figure from this tree, not #2011's.
- **`boot5.sh`'s `RECIPE_BRANCH` default is re-pointed to `main`** — the branch the recipe
  lives on once this merges. A default naming a session branch is a landmine the day that
  branch is reset or deleted after its squash-merge, the exact shape of the stale-URL defect
  that block already post-mortems. ⚠ **UNTIL THIS PR MERGES, a sandbox boot must pass
  `RECIPE_BRANCH=claude/handoff-docs-review-mj1oxt`**, or it fetches `main`'s recipe — which
  predates the `C_zoom` producer and the `meas_wall.py` outlier rejection — and the extraction
  guard will NOT catch that, because `main` already defines all 27 MAP keys in their pre-fix
  form. *A guard that asserts the set does not assert the contents.*
- **The BYO rewrite (§4 item 5) is done, §3a.** Verified against the code before acting, 18
  lines across the five coach docs, zero residual.
- **And this file now records what MERGED since the last handoff** (§1a, #2008 → #2011). The
  first draft recorded only the unmerged work; `HANDOFF-2026-09-01.md` stops at #2006 and
  #2007 was itself, so four merged PRs sat in no handoff at all. *A range claim is a claim —
  and a range gap is one too.*

The first session's own opening follows, unedited except where a ⚠ says otherwise.

⚠ **NOTHING FROM THIS SESSION IS MERGED, AND THERE IS NO PR.** Measured before writing:
`list_pull_requests` returned `[]`. Six commits sit on two `claude/*` branches, both
ahead of `main`, neither with a PR open:

| Branch | Ahead of `main` | What |
|---|---|---|
| `claude/shape-radio-launch-cut-18jr67` | **4** — `926ba40` `cf884db` `cfea985` `acd7a7c` | the render pipeline fixes and v7.2 |
| `claude/worklog-review-ozn07z` | **2** — `0039525` `ec0b8a7` | the orb-card record, the launch-video brief + end-card build |

`#2011` merged as `da2c8ad` and **both branches were reset onto it afterwards**, so these
commits are fresh work, not stacked on merged history. ⚠ **`marketing/shape-radio-watch-orb.md`
exists on BOTH branches** (brought across in `acd7a7c`, then amended there); the launch-cut
branch's copy is the newer one. Merging both cleanly means taking the launch-cut version.

⚠ **THE SESSION SPANNED 2026-09-03 → 2026-09-07 AND THE OWNER REDIRECTED IT SEVEN TIMES.**
Marketing docs review → Higgsfield video generation → the #2011 render → *stop working on
#2011* → marketing docs → *no, videos* → the launch-video end card → *lift the #2011
restriction, finish the render*. Every redirect is in the record; if a file looks half-done,
check whether it was cut off by a redirect before assuming it was abandoned.

**The three handoff rules, still binding:** no `main` SHA is recorded here; "nothing in
flight" cites the **open-PR list**; and a suite figure is a re-run, never carried — ⚠ **the
suite was NOT re-run this session** (every commit was docs-only and the pre-commit hook
skipped it), so the last measured figure is 2643/2643 from 2026-09-02 and is **stale**.

---

## 1. State snapshot

| | |
|---|---|
| `main` | read it: `git fetch origin main && git rev-parse HEAD origin/main` |
| Working branch | `claude/handoff-docs-review-mj1oxt` — `origin/main` + the six docs commits, consolidated (§0) |
| Open PRs | **none** — measured twice: by the first session before writing, and again in the second session before this update (`list_pull_requests` → `[]`). ⚠ This branch's own PR makes the row stale for exactly one entry. |
| Suite | **2646 / 2646**, 0 fail — ⚠ **re-run 2026-09-07 on THIS tree**, replacing the stale 2643 the first session carried (2643 + the three `radio-hr-sync-labels` tests #2011 added) |
| `tsc --noEmit` | exit 0 — the stripe `apiVersion` error `HANDOFF-2026-09-01.md` §4 records did not reproduce; the SessionStart hook installs deps |
| Migrations owed | **zero** — nothing on this branch touches a schema; the newest file is still `2026-08-31-tier-reward-claim-cost.sql` |
| `public/m` | untouched |
| Code touched | **none** — every commit is `docs/` or `marketing/` |
| i18n | 13 locales × 18 namespaces × **4,162** `en` keys = **54,108** values (was 4,161 — #2009 added `profile:ridge.topTier`) |
| The i18n ratchet | `tests/i18n-surface-inventory.test.mjs` **9/9**, every column unchanged since 09-01: 359 rendering JSX · 115 fully covered · 34 partial (168 strings) · 96 with no translator (818) · 114 with no copy. That is the certification: nothing in #2008 → #2011 or on this branch adds or drops member copy, so the measurement had to move by nothing. |

### 1a. Merged since the last handoff — #2008 → #2011, four PRs

Derived from `git log origin/main` after the commit that added `HANDOFF-2026-09-01.md`
(`705fd6d`, #2007). Not this session's work, but inside the range and therefore this file's to
record.

| PR | `main` | What |
|---|---|---|
| #2008 | `9d596b47` | 2026-09-01 — the **blueprint** and **concrete** textures voided the page background on all 18 papers (a bare `rgba()` in a non-final `background` layer drops the whole declaration; invisible on an ordinary page, catastrophic on the Settings overlay, which painted over a live Home). `tests/theme-texture-css.test.mjs` evaluates the shipped `makeTexture` in Chromium's parser terms. Also corrected the newest-handoff command from `ls -t` to a lexical sort. |
| #2009 | `6815bcd0` | 2026-09-02 — the Legend ascent chart labelled both ends of a segment that does not exist; re-framed as the **whole ladder** on both member profiles (mobile + `livingDesktop.jsx`, same `0.82` cap). Coach pills compacted without shrinking their tap target. `profile:ridge.topTier` ×13. |
| #2010 | `66cf5474` | 2026-09-03 — the auto-loaded `WORKLOG.md` had reached ~400k tokens; split into dated archives, byte-lossless, with `Open work` promoted above the changelog. |
| #2011 | `da2c8ada` | 2026-09-03 — the launch-cut recipe v3 → v7.1 and `social-video-ideas-wave-2.md`; the Radio HR card's triple *In sync* collapsed to one verdict (`iosAppBroadsheetRadio.jsx`, `tests/radio-hr-sync-labels.test.mjs`); the War Room marketing section. The one shipping-code change in the range. |

**Reviewer record, read not inferred:** Codex auto-fired on all four with trigger *PR opened*
and was clean on #2008 (`2868dab`), #2009 (`2e96b82`) and #2010 (`6bbfc7a`). On #2011 it
reviewed `c40e58b` with **two P2 findings, both real and both fixed** in `b4e658d` — the merged
head, which Codex did **not** re-review (one review object on the PR, pinned to `c40e58b`).
Under the head-pinning rule that verdict is *stale* on the merged head; the gate (CI green on
the final head AND not a draft) did not require it. Nothing was hand-triggered — the compliant
shape. CodeRabbit posted only its fewer-than-10-stars skip notice on each.

---

## 2. The launch cut — where it actually is

**The v7.1 pipeline had never produced a render.** It has now, three times, and the
third is the honest one. Read
[`marketing/shape-radio-launch-cut.md`](../marketing/shape-radio-launch-cut.md)
§"What v7.2 is" and [`marketing/shape-radio-watch-orb.md`](../marketing/shape-radio-watch-orb.md).

### What was blocking it, in the order it fell

Every one was found by **running the documented pipeline on a clean sandbox**, not by
reading it. Each is recorded at its own site in the recipe.

1. **`boot5.sh` fetched the recipe from a stale branch** — a 2,529-line v6-era copy while
   the MAP named v7 scripts. 6 of 26 keys never extracted; `render6.sh`, `verify6.py`,
   `mk_watch.py` extracted as their **v6** versions. The guard printed a file count that
   went UP on the failure. Fixed: URL tracks `RECIPE_BRANCH`; the guard asserts the SET.
2. **`cap/r3_radio_top.png` had no producer** — read by two scripts, written by nothing.
   Fixed: `pw/cap_radio.js`, in the MAP, proven on the live site.
3. **`in/C_zoom.mp4` had no producer** and its `zoompan` was recorded without `N` or the
   anchor. Recovery exhausted (both branches, `git log -S`, transcripts): **introduced
   already incomplete** in `e22dead`. Re-derived — `N=200`, centred — and corroborated:
   median slab 1127 px vs the recorded ~1165. ⚠ **The refusal that exposed it was a
   `meas_wall.py` defect, not the zoom**: it dropped frames that don't read but kept
   frames that read *badly* — one mid-strobe frame collapsed the strict intersection to
   132 px. Outlier rejection before the intersect; measured after: 1010 wide.
4. **`captions.py` and `cap_radio.js` were missing from the run order** — `mk_globe.py`
   dies nine steps in without `cap/txt/close.png`.
5. **`in/phoneB.mp4` had no producer**, and `mk_screen3.py` (which might have) is
   documented under a `###` heading the extractor doesn't match. Owner ruled: **carry
   the most recent layer** (the v5 montage). Building that found **`params_v4.json` has
   no producer either**, the v5 montage names a **`home` segment the v5 tours never
   record**, and the montage **does not fit v7's Scene B** (6.03 s into 6.33 s, forcing
   the start to the instant the phone lands). Owner-eye item, §4.

### What the owner ruled on the film

- **The two-orb card IS the beat match; the physical watch is gone.** `A2` is Scene A
  continuing from `f12`; the beat-12 cut is invisible; `meas_watch.py`/`mk_watch.py` are out.
- **Runner:** job `878ea5a1` — wide, locked off, side profile, clothed, explosive. Four
  rounds; each note was a defect in *my prompt* (§"Runner" in the orb record).
- **Music: the tracks already made.** All seven measured with a driver validated against
  the three recorded grids (Δ ≤ 0.05 BPM). ⚠ **`sonilo_music` ignores tempo requests** —
  six of seven landed at ~128 whatever was asked (120/122/124/145/150/155). A tempo is
  imposed by `atempo`, then **re-measured**. `m1` × 1.09375 = a true **140.00**.

### ⚠ The render I shipped, then caught

`https://litter.catbox.moe/oeayld.mp4` (72 h from 09-03, likely dead) is the first v7.2
render — and **its card reads `140 / 140 · IN SYNC` over `t3`, which plays 119.45 BPM.**
`BPM` was hardcoded from the standalone preview's separate 140 bed. That is the
fabricated readout the recipe's own v6 note forbids. Fixed at the root: `BPM` is
**derived from `meas_<track>.json`** — the same file the grid comes from. The corrected
render (`BPM 119`, `HRM 83→119`) built twice and **was lost twice to sandbox reclamation
before upload**; a third attempt was reclaimed before it logged its first line. **So the
corrected cut exists only as a script, not yet as a link** — it is the first thing to run
in the next session (§5 for how to survive the lease).

---

## 3. The other deliverables

- **Launch-video brief:** `marketing/launch-video-prompt.md` (was chat-only), sectioned
  ×9, with a production-notes section of measured facts.
- **End card (shot 12):** `marketing/launch-video/build-endcard.sh`, media gitignored.
  ⚠ The wordmark canvas is 3626×882 but the ink is **3082×342** — scaling the canvas to
  55 % renders the mark at **46.8 %**. The script crops to the ink first. Alpha verified
  by decoding the pixels (94 % transparent). `SHAPE-wordmark-white.png` is
  **byte-identical** to the primary, not a fallback.
- **Marketing docs review:** eleven findings, tasks #1–#11, untouched since. The one
  that matters: **all five coach docs sell a flat 15 % on everything; the product has
  shipped BYO 0 % since 2026-07-21** (`dashBusiness.jsx:237`). Every recruiting email's
  wedge understates the offer. **→ Fixed 2026-09-07, §3a.**
- ⚠ **ONLY #1 OF THOSE ELEVEN IS WRITTEN DOWN ANYWHERE.** The other ten lived in the first
  session's task list and never reached the repo, so they are **not recoverable** from this
  record. *A finding that exists only in a session's task list does not exist* — the same
  rule the orb record states for a sandbox. If they matter, re-derive them from a fresh read
  of the five docs; do not assume this file lost nothing.

### 3a. The BYO correction — done this session, 18 lines across the five coach docs

Verified before acting: `public/newdesign/dashBusiness.jsx:237` reads *"Clients a coach
brings pay 0% Shape commission; marketplace-delivered clients pay 15%"*,
`src/lib/platform-fee.ts` says the same (owner-ruled 2026-07-21), and
`supabase-migrations/2026-07-21-coach-byo-commission-split.sql` is what shipped it. Meanwhile
`coach-acquisition-campaign-plan.md` · `coach-marketing-campaign-plan.md` ·
`coach-nurture-email-sequence.md` · `coach-outreach-email-sequence.md` ·
`coach-recruiting-campaign-plan.md` carried **18 lines** saying *"keep 85%"* or *"a flat 15%,
only when a client pays you"* — every wedge understated the offer by the whole BYO half.

**The rule every corrected line now states:** the clients you bring (in-app invite or your own
share link) pay **0 %** and you keep your full rate; Shape's flat **15 %** applies only to
clients its marketplace sends you, and only when they pay you; members pay their own $5/mo; no
monthly software fee, no setup fee. The one caveat the code carries — a member already in the
coach's Shape waiting room counts as Shape-found (`DBZ_BYO_PITCH`) — is recorded in the
marketing plan's *"verify claims before posting"* line (now dated 2026-09-07), not in the email
bodies: fine print belongs where the sender checks, not in a cold open. **18/18 replaced; a
grep for `85%` / `keep 85` across the five reads zero.** ⚠ The new wedge lines are
house-register copy, not the owner's words — they want the owner's eye before any send,
exactly as the caption copy does.

---

## 4. Open follow-ups, ranked

1. **Get the corrected render to a link** (§5) and **re-derive `verify6.py`'s
   thresholds against it** — the watch assertions test for a shot that no longer exists.
   Then open **one PR** for `claude/handoff-docs-review-mj1oxt` — both branches' work sits
   on it now (§0); docs-only, merges on CI green. ⚠ **Until it merges, boot the sandbox with
   `RECIPE_BRANCH=claude/handoff-docs-review-mj1oxt`** — the default now reads `main`.
2. **Owner's eye: the phone in the cut.** Logo-only shipped (v6 behaviour). The 4-page
   montage (`deck · menu · feed · profile`, 1.155 s of logo each side) is built and
   recorded but **not rendered** — capture + render don't fit one sandbox window.
3. **`verify6.py` flagged four things worth a look** on the first render: the wall screen
   fills **0.73 w** of a measured 1010 px slab (`mk_wall6` sized it 803); last frame luma
   29 (not black); audio tail −16 dB vs an expected ~−18; and beat-12 "both cut frames"
   — now moot with the cut gone.
4. **EAT re-capture (#19) is untouched.** Needs a new part F in `body5b.js` ticking two
   dishes → Merge the mise → scroll; `verify5.py spot eat` must re-run.
5. ~~**The BYO rewrite of the five coach docs** (#1)~~ — **done 2026-09-07, §3a.** What
   remains before a send is the owner's read of the new wedge lines.

---

## 5. ⚠ Two environment facts that cost this session hours

**The Higgsfield sandbox lease is ~4–6 minutes, not the 15 the tool description claims,
and every call after the first starts a FRESH sandbox.** Six full pipeline runs were lost
to reclamation, two of them *after* the render completed and before upload. What works:
**one background script that does everything** (boot → layers → render → upload) with
the upload as the very next line after the render, and hosts tried in the recipe's order
(uguu → litterbox → gofile). What does not: anything split across calls, and any run that
includes the ~5-minute app capture *and* a render.

**This container's egress proxy DENIES cloudfront, catbox, 0x0.st and litterbox** (403 on
CONNECT). Media can be fetched, rendered and md5'd only in the sandbox; the owner opens
the links, not the agent. `npm i ffmpeg-static` gives a local ffmpeg 7.0.2, but nothing
to feed it unless the owner uploads a clip into the chat.

---

## 6. The other HANDOFF files

`ls docs/HANDOFF-*.md | sort -r | head -3` — this file,
[`HANDOFF-2026-09-01.md`](HANDOFF-2026-09-01.md) (#1992 → #2006) and
[`HANDOFF-2026-08-31b.md`](HANDOFF-2026-08-31b.md) (#1988 → #1990).
