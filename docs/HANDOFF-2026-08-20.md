# Handoff — 2026-08-20

Two merges close the cook wave: **#1911 → `3c4ee6e9b`** (Shape Kitchen 100 + the
passive-window overlay) and **#1910 → `056cd0279`** (cook together: three options, three
schedulers). Both branches kept.

Read this alongside `docs/WORKLOG.md` (the two dated entries at the top) — this file
carries what the changelog does not: what is still open, what was refuted, and the traps
that cost the most time.

---

## 1. State

- `main` — verify with `git fetch origin main && git rev-parse --short origin/main`. Do
  not trust a SHA written here; a handoff is committed before its own squash.
- Suite **2100**, `tsc --noEmit` clean. CI green on the final head.
- ⚠ **THE REPO ENCODES TWO DIFFERENT MERGE GATES, and you should know which you are
  following.** `docs/WORKLOG.md` says *CI green AND **Codex** clean on the final head*.
  `src/lib/console-flight.mjs` — the gate Mission Control actually computes, carrying an
  **owner ruling (2026-07-27)** — says *CI green AND **CodeRabbit** approved-or-clean on
  the head AND **Codex present***, where presence is deliberately **not** freshness
  (Codex leaves no record when clean, so head-pinning it would jam every clean pass at
  `none`).
  ⚠ **The open question is whether CodeRabbit GATES — not whether it runs.** It runs on
  request, the owner authorised that 2026-08-19, and you MAY wait on it: the wording this
  file used to carry, *"neither a gate nor a layer to wait on"*, is **withdrawn**. What is
  unsettled is that `coderabbitVerdict` is **head-pinned**, so the ratified "CodeRabbit
  ONCE" sweep stops counting the moment you push a fix for its own findings — the process
  and the shipping code cannot both be satisfied without a re-review. That is an **open
  owner ruling**, not a merge-time decision; say which gate you applied.
- **#1910 merged green under the console's gate**: CI green and CodeRabbit clean on the
  final head, Codex present from `f5d2ef80c`. Under the WORKLOG's stricter sentence it
  would not be. ⚠ I first wrote this file recording it as a **rule break by me**; that
  was **wrong** — I had trusted the WORKLOG's wording without checking the shipping code.
  Codex was re-triggered on 2026-08-20 anyway, so the strict reading is satisfiable too;
  check #1910 for its verdict.
- **No migration** in either PR. Both are source-module + engine changes.
- Open PRs: none from this wave.

## 2. What shipped

**The prep sheet asks WHY before it starts, and the answer changes the schedule.** The
owner ruled the three options are three *different questions*:

| Option | Means | Runs |
|---|---|---|
| Cook at the same time | weave the work into the gaps | `TOGETHER` |
| Cook separately | finish one, start the next | `SEQUENCE` |
| Cook to serve | everything completes at one moment | `SERVE` |

⚠ **NOT "the shortest session" — this table said that, and it was the same unsupported
superlative #1913 deleted from the option's own subtitle.** TOGETHER weaves whatever
windows exist and runs **no order search**, so it cannot claim an optimum; it is also
measured finishing **later** than SERVE in **7.7%** of pairs. The shipped copy now reads
*"less time in the kitchen"*, which is supportable. ⚠ A superlative removed from the
screen can survive untouched in the record that describes the screen — check the record
when you delete a claim from the UI.

"Cook at the same time" had been running **SERVE with no serve time** — so two of the three
doors led to the same room, and the option meant to get the cook *out of the kitchen* was
optimising for landing dishes together. Measured over **4,950 catalog pairs** — every pair
of the merged 100-recipe catalog — TOGETHER finishes sooner than SERVE in **29.5%** and
later in **7.7%**; it is the faster plan on balance, but the reason it is correct is that
it is a different question.

⚠ **Do not quote #1910's merge message for these.** It says 3,570 pairs / 16.0% / 4.2%,
and 3,570 is exactly C(85, 2) — that measurement predates #1911 and omits all 1,380 pairs
involving the 15 new recipes.

**The engine.** One placement now answers for the timeline, the serve time *and* the
issues. A pull names the resource that caused it. A serve search that runs out of
iterations falls back to the serial bound rather than returning an empty timeline, and
clears `exact` when it does.

## 3. The honest limits — read these before promising anything

- ⚠ **"Cook to serve" does not usually deliver a single moment.** It lands every dish
  together in **7.4%** of pairs (mean gap **13.5 min**). It is **overwhelmingly not the
  kitchen** — unlimited stations reach only **8.6%**. It is the **one cook**, who cannot
  finish two hands-on dishes at the same instant. ⚠ An earlier version of this file said
  unlimited stations measure *identically*; on the 85-recipe catalog both arms read 2.2%
  and I took equal numbers for proof. Re-measured they are 7.4% against 8.6% — the
  conclusion survives, the word does not. The sheet discloses the gap beside the
  start time. **Closing this is a scheduler design change**: model the cook as the scarce
  resource and optimise against that. Do not describe it as a tuning job.
- ⚠ **71 of 85 recipes still schedule short** against their own annotations (1,817 min
  total). The scheduler honours what the annotations say; the annotations understate what
  the recipes do. **This is a DATA pass over step text, not an engine one.**
  ⚠ **`71 of 85` IS THE OLD CATALOG** — measured before #1911 took it to **100**, so the 15
  recipes imported via `USDA2_KITCHEN_RECIPES` are **unaudited**. Resolving the 71 does not
  close this residual; re-measure across all 100 first. A stale denominator is how a
  residual gets closed while a sixth of the catalog was never looked at.
- ⚠ **The pull reason code has no UI consumer.** `BS_SERVE_ISSUE.STATIONS` vs `.COOK` is
  correct now, but nothing on screen tells a cook which one held their plan apart.
- ⚠ **Tuna macros are still an OWNER RULING**, carried from the USDA wave.
- ⚠ **The serial fallback is unreachable on real data** — it needs a recipe past
  `BS_ORCH.serveSearchMax` steps where the catalog's largest is **8**, and the search is
  already ~17s at 320 steps. It makes the failure honest; it does not make that session
  usable. Do not cite it as a capability.

## 4. What review cost, and the two things worth carrying

Findings ran **27 → 6 → 5 → 3 → 0** across five rounds.

**Three of the last four rounds found a defect inside my own preceding fix.** Both worth
knowing because they are the same shape:

1. The landing gap was moved to a better screen and quietly started describing a
   *different plan* than the start time beside it — 5.7% disagreement, one-directional,
   understating by up to 101 min. **When you move a number to a better place, check it
   still belongs to what it now sits next to.**
2. The serial fallback fixed an empty timeline and then handed the sheet an **upper bound
   with `exact` still true** — a proof claim on a number that was only "one that works".

**Three of my own tests were hollow, and only mutation-testing caught them.** The options
test asserted row badges rather than the plan actually run; the harness-isolation guard
used identically-shaped components, which share a cell array quite happily; a hold fixture
had stopped reaching the case it existed for after the catalog merge. **None were caught
by review or by a passing suite.**

## 5. Two findings refuted — do not "fix" these again

- **"The serial fallback can itself fail."** Mechanism real, consequence not: the named
  case (100 one-minute steps against 1) returns a complete 101-event timeline, and 11
  shapes converge including 7- and 8-dish sets past the exhaustive permutation bound. The
  uneven pair is pinned as a regression case.
- **"The Pidgin subtitle is a sentence fragment."** So are all 13 locales, the English
  source included (*"On the table at a time you choose"*) — it sits under the option
  title. ⚠ The *adjacent* Pidgin finding **was** taken: `by {t}` for a clock time
  contradicts the locale's **own** convention (`for {time}`, three instances across coach
  and marketplace).

## 6. Traps that cost real time this session

- **The mount harness installs no translator**, so copy renders with placeholders intact
  (`Within {n} min of each other`). That is deliberate —
  `tests/kitchen-allergen-surfaces.test.mjs` asserts on the literal template. To assert on
  a real number, install an interpolating `window.ShapeI18n` **scoped to one test**, as
  `tests/cook-serve-schedule.test.mjs` does. Do not change the shared harness.
- **CI builds the PR merge commit**, not the branch head. A branch green on its own can go
  red the moment something merges to `main`. When it does, re-pin catalog-dependent tests —
  and check the re-pinned test still *discriminates*; one of ours was one edit away from
  becoming decoration.
- **`\n` inside a shell heredoc collapses.** Build multi-line anchors with `chr(10)`, or
  write the script to a file first. This cost two broken edits today.
- **Inserting into `warroom.ts`**: anchor on the object's opening `{`, not on its
  `section:` property — a sibling object dropped inside an object literal is a parse error.
- **A War Room `status` flip is not enough.** If the label still says "NOT BUILT", the
  entry now contradicts itself. Rewrite the label, and say what shipped *differently* from
  what the item asked for (ours specified a disabled-with-reason state; the sheet discloses
  the gap instead).
