# Handoff — 2026-08-20 (session 2)

Picks up where `docs/HANDOFF-2026-08-20.md` leaves off. That file describes the **merged**
cook wave; this one is the state of the **two open PRs** and what is still owed.

---

## 1. Where things stand

| | PR | branch | state |
|---|---|---|---|
| Records | **#1912** | `claude/records-2026-08-20` | open — Codex gate triggered |
| Follow-ups | **#1913** | `claude/cook-together-followups` | ✓ **MERGED → `5972b786e`** |

⚠ **NO HEAD SHA IS RECORDED HERE, DELIBERATELY.** An open PR's head moves — including by
the very commit that adds a note like this one — so a SHA written down beside "awaiting a
verdict" is stale before anyone reads it, and the next session can then accept a verdict
that predates the work it is supposed to cover. **Fetch the live head** (`GET
/repos/:owner/:repo/pulls/:n` → `head.sha`, or `git rev-parse origin/<branch>`) and check
the verdict against THAT. The only SHAs safe to pin are merged ones, which cannot move.

#1913 had a CodeRabbit sweep on an earlier head and Codex clean on its final head. #1912's
CodeRabbit sweep is likewise on an earlier head — see §2 on why that cannot satisfy
`prAllGreen` without a re-review, and why that is an open ruling rather than a merge-time
decision.

Merged this session: **#1913 → `5972b786e`**, **#1910 → `056cd0279`**, and (yesterday)
**#1911 → `3c4ee6e9b`**.

## 2. The reviewer system — ratified by the owner 2026-08-20

> *"ok put this system in place moving forward."*

1. **Own adversarial self-review** before the first push.
2. **CodeRabbit ONCE**, as a breadth sweep — one `@coderabbitai review` on a head you
   believe final.
3. **Codex — THE GATE.** The merge waits on its verdict.

⚠ **Not both every round** — that spends two finite budgets on one question.
⚠ **Never compare their severity labels.** "Major" (CodeRabbit) and "P1" (Codex) are
self-assigned on different scales; never sum or rank them against each other.

Recorded in `docs/WORKLOG.md` (top of the review-stack paragraph, which has flip-flopped
four times — the ruling goes FIRST) and in the `reviewer-system` memory.

⚠ **The repo encodes TWO different gates and they disagree.** `docs/WORKLOG.md` says *CI +
Codex clean on the final head*; `src/lib/console-flight.mjs` computes `prAllGreen` = *CI +
CodeRabbit approved-or-clean + Codex PRESENT*, where presence is deliberately **not**
freshness under a 2026-07-27 owner ruling. **Read the shipping code before declaring a gate
met OR broken** — I got this wrong in both directions in one session.

## 3. What #1913 contains

Three findings against merged `main`, then two more rounds on top:

- **Two options ran the same plan.** With no window to weave into, TOGETHER falls back to
  serial — "cook at the same time" produced the same timeline and printed the same minutes
  as "cook separately" while promising simultaneous cooking. Now unavailable-with-reason,
  minutes suppressed.
- **A carried timer vanished.** ⚠ The expiry filter lived at **three** sites — display,
  handoff, wrap. I fixed one, then a second round found the other two.
- **"Out of the kitchen soonest" was unsupported.** TOGETHER is order-sensitive; the order
  search that would justify a superlative is on the SERVE path only. Copy changed in the
  **six** locales that actually claimed an optimum. ⚠ **The remainder is SEVEN, not five**
  — this file said five and left two locales unaccounted for; 6 + 7 = 13, the whole set.
  Counted off the merged diff, not restated.
- **An owner ruling routed to the ENG lane.** `OWNER RULING NEEDED —` matched no pattern in
  `console-triage.mjs`. Fixed at the classifier; the neighbouring ruling routed correctly
  only *by accident* (it contains the word "unruled").

## 4. What #1912 contains

Records for both merges, plus a re-measurement:

⚠ **Every headline figure was measured over C(85,2) = 3,570 pairs and is superseded.**
Re-measured over all **4,950** pairs on the merged 100-recipe catalog:

| | was | now |
|---|---|---|
| TOGETHER sooner than SERVE | 16.0% | **29.5%** |
| TOGETHER later | 4.2% | **7.7%** |
| means TOG / SERVE / SEQ | 38.6 / 39.9 / 44.4 | **51.9 / 53.9 / 64.4** |
| "cook to serve" lands together | 2.2% | **7.4%** |
| mean gap | 15.6 | **13.5 min** |

⚠ **One claim withdrawn:** that an unlimited-station kitchen measures *identically*. It is
**7.4% vs 8.6%** — the cook is still the binding constraint (~91%), but two equal numbers
on the smaller catalog were a **saturated result** hiding a real difference.

⚠ **#1910's merge message is immutable and now wrong on these numbers.** Every record says
so and points at the corrected figures. **Do not quote it.**

## 5. Still owed — owner rulings, not code

- **Tuna macros.** Source figures do not reconcile with the dish as plated.
- **A third allergen class.**
- **Source fidelity** — three of six sourced recipes diverge from their source pages while
  carrying that source's nutrition verbatim; one adds soy sauce (a gluten source) to a
  recipe already flagged as an ambiguous gluten-free case. **This is the one I would least
  like left sitting** — it is live data on published recipes.

## 6. Registered, not built

- **A true single-moment serve is a scheduler redesign**, not a fix. The binding constraint
  is the one cook; unlimited stations barely move it.
- ⚠ **71 of 85 recipes schedule short** (1,817 min) — and **`85` is the OLD catalog**. The
  15 recipes from `USDA2_KITCHEN_RECIPES` are **unaudited**; resolving the 71 does *not*
  close this. Re-measure over all 100 first.
- **The pull reason code** (`BS_SERVE_ISSUE.STATIONS` vs `.COOK`) has no UI consumer.

## 7. Traps this session actually cost time

- ⚠ **`git checkout -- <file>` to undo a mutation wiped every uncommitted change in that
  file.** Already recorded in this repo; walked into anyway. **Mutation-test with `cp`,
  never with git**, and commit before starting.
- ⚠ **A fix that mutation-survives is not a fix.** Two of this round's changes left the
  suite green when reverted — coverage existed for a sibling site only. **Revert every fix
  and watch it fail before believing the test.**
- ⚠ **An alternation in an assertion hides which branch fired.** `/wait long enough|free
  burner/` passed for either reason code, so the test never exercised the case it was
  written for.
- ⚠ **A number in four records needs fixing in four records.** "I already corrected that"
  was true of one of them.
- **`\n` in a shell heredoc collapses**; build multi-line anchors with `chr(10)` or write
  the script to a file. **An apostrophe inside a single-quoted JS string** (`#1910's`)
  breaks `warroom.ts` — `tsc` catches it.
- **`grep -E "^. (pass|fail)"` cannot match the multibyte `ℹ`** node prints. Use
  `grep -E "(pass|fail) [0-9]+"`.
- **The mount harness installs no translator**, so copy renders its `defaultValue`
  verbatim — a catalog change without the matching `defaultValue` edit shows the OLD text.

## 8. First moves next session

1. Read the Codex verdict on **#1912** — against its **live** head, fetched at the time you
   look, never a SHA copied out of a record (§1).
2. Work anything real; merge on green-and-clean per the standing rule, **keeping the
   branch**. #1913 is already merged and its branch kept.
3. `public/m` is gitignored and built at deploy — rebuild locally to verify, never commit.
